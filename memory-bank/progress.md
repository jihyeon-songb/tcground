# PROGRESS

> 작업 진행 상황과 의사결정 로그.
> 마지막 갱신: 2026-06-16 (일일 가격 수집 안정화)

## 현재 작업

- 없음.

## 완료 로그

- 2026-06-16: eBay Marketplace account deletion/closure notification 검증 endpoint를 추가했다. `app/api/ebay/account-deletion/route.ts`는 eBay가 보내는 `challenge_code`를 받아 `challengeCode + EBAY_DELETION_VERIFICATION_TOKEN + EBAY_DELETION_ENDPOINT` 순서로 SHA-256 해시한 `challengeResponse` JSON을 반환하고, POST 알림은 현재 저장 중인 eBay 사용자 계정 데이터가 없으므로 200 ACK만 반환한다. `.env.example`에 `EBAY_DELETION_VERIFICATION_TOKEN`, `EBAY_DELETION_ENDPOINT`를 추가했고, route 테스트 5개를 추가했다. `pnpm exec vitest run app/api/ebay/account-deletion/route.test.ts`, `pnpm exec tsc --noEmit`, `pnpm test --run`, `pnpm lint`(기존 `packages/headless/dist` warning 7개)가 통과했다.

- 2026-06-16: KREAM 검색 페이지 daily 수집을 단일 검색어 초기 약 50개 한계에서 catalog 기반 segmented search로 확장했다. `collect:kream:daily`는 이제 `--segmented --max-scrolls 0 --query-delay-ms 750`으로 실행되며, `scripts/collect-kream-search-page.ts`가 기본 검색어와 catalog 세트명/세트명+레어도 검색어를 순회해 product id로 중복 제거한 뒤 기존 confidence 매칭으로 `kream_search_page_asking` snapshot을 만든다. 테스트 제어용 `--max-keywords`, `--navigation-timeout-ms`, `--search-timeout-ms`도 추가했다. focused KREAM test, `pnpm exec tsc --noEmit`, `pnpm test --run`, `pnpm lint`가 통과했다. 실제 KREAM dry-run은 반복 자동 접근 상태에서 0건 응답이 나와 쓰기 검증은 하지 않았다.

- 2026-06-16: KREAM 검색 페이지 daily 수집의 고정 개수 제한을 제거했다. `scripts/collect-kream-search-page.ts`는 이제 기본적으로 `--limit` 없이 실행되며, product link 수가 더 이상 증가하지 않을 때까지 스크롤한다. 무한 루프 방지를 위해 `--max-scrolls`만 남겼고, `pnpm collect:kream:daily`는 `--max-scrolls 120`으로 실행된다. KREAM이 자동 브라우저에 500/빈 페이지를 간헐적으로 반환하는 경우를 대비해 페이지 로드를 최대 3회 재시도하고, 그래도 상품 link가 없으면 0건으로 종료하게 했다. 현재 2026-06-16 로컬 Supabase에는 앞선 성공 실행에서 적재된 `kream_search_page_asking` snapshot 15건이 유지된다.

- 2026-06-16: KREAM/eBay daily cron이 launchd에서 실행되지만 장시간 실패하거나 snapshot을 적재하지 못하던 문제를 보정했다. `pnpm collect:daily`의 eBay 경로를 전체 카탈로그 처리에서 날짜별 50장 rotating window로 바꾸고, `scripts/collect-prices.ts`에 `--daily-window-size`를 추가했다. eBay Browse에는 8초 fetch timeout을 적용했고, API 기반 `ebay_browse`/`kream` source는 연속 실패 threshold를 넘으면 batch를 abort하게 했다. 기존에 멈춰 있던 LaunchAgent를 내리고 재등록한 뒤 `launchctl kickstart`로 검증했으며, 새 실행은 exit code 0으로 종료되고 DB에 `ebay_browse` succeeded batch 2개와 API 기반 `kream` partial batch 1개가 기록됐다. 이후 KREAM 검색 페이지가 실제 브라우저에서 상품명/product id/현재 표시가/거래 수를 DOM으로 제공하는 것을 확인하고, `scripts/collect-kream-search-page.ts`와 `lib/pricing/kream/search-page-adapter.ts`를 추가했다. 검색 결과 40개 dry-run에서 12개가 안전 매칭됐고, 실제 실행으로 `kream_search_page_asking` snapshot 12건을 2026-06-16 로컬 Supabase에 upsert했다. 현재 `.env.local`은 eBay sandbox라 eBay snapshot은 0건이다.

- 2026-06-15: 일일 가격 수집 LaunchAgent가 실행 후 `env: node: No such file or directory`로 종료되어 6월 5일 이후 새 snapshot이 생기지 않던 원인을 확인하고 수정했다. `pnpm` 실행 파일의 shebang이 `/usr/bin/env node`라 launchd 기본 PATH(`/usr/bin:/bin:/usr/sbin:/sbin`)에서는 Node를 찾지 못했으므로, `scripts/launchd/com.tcground.daily-price-collection.plist`와 설치본 LaunchAgent에 Node 22 bin 경로를 `EnvironmentVariables.PATH`로 추가하고 재등록했다. 재검증 결과 launchd 환경에서 `pnpm --version`과 `node --version`이 정상 출력되고, repo/설치 plist `plutil -lint`가 통과했다. 추가 확인 결과 현재 `.env.local`은 로컬 Supabase와 eBay sandbox를 가리키며, 설정된 service key는 로컬 price table 읽기/쓰기에서 permission denied가 나므로 실제 적재 전 DB target/key 정합화가 필요하다.

- 2026-06-15: KREAM/eBay 가격 데이터 일일 수집 경로를 추가했다. `package.json`에 `pnpm collect:daily`를 추가해 eBay Browse asking과 KREAM asking을 `--source-batch-size 100`으로 수집하게 했고, `scripts/launchd/com.tcground.daily-price-collection.plist`로 macOS LaunchAgent 매일 03:00 실행 템플릿을 추가했다. 실제 사용자 LaunchAgent(`/Users/songjihyeon/Library/LaunchAgents/com.tcground.daily-price-collection.plist`)에도 등록했고 `launchctl print`로 calendar trigger를 확인했다. `package.json` 파싱, repo/설치 plist `plutil -lint`, `pnpm exec tsc --noEmit`, focused Vitest 25개, `pnpm lint`(기존 `packages/headless/dist` warning 7개)가 통과했다. 실제 외부 수집은 API/DB 쓰기와 KREAM 접근 제한이 걸려 있어 즉시 kickstart하지 않았다.

- 2026-06-15: KREAM 자동 가격 수집을 체결(sold) observation 적재에서 판매중 호가(asking) snapshot 적재로 전환했다. `collect-prices`의 `kream` source는 이제 `kind='asking'`으로 동작하며 `collectKreamAskingSnapshots`가 KREAM product의 sales option/ask payload를 `kream_asking_median` snapshot으로 줄인다. `kream`은 asking source 목록에 추가했지만, 기존 `manual_kream` CSV sold evidence와 과거 `median_filtered` KREAM snapshot은 `aggregation_method` 우선 판정으로 sold 참조점에 남긴다. focused test 56개, `pnpm lint`(기존 `packages/headless/dist` warning 7개), `pnpm exec tsc --noEmit`, `pnpm test --run`(291/291)이 통과했다. `pnpm format:check`는 저장소 전체 기존 포맷 이슈 66개로 실패해 변경 코드 파일만 Prettier 적용했다.

- 2026-06-15: `npm run dev` 실행 실패 보고를 점검했다. 프로젝트 표준 명령은 `pnpm dev`이며, 현재 로컬 Next dev 서버가 이미 PID `32264`로 `localhost:3000`에서 실행 중이고 HTTP 200으로 응답함을 확인했다. 같은 프로젝트에서 새 dev 서버를 중복 실행하면 Next가 `.next/dev/logs/next-development.log`의 기존 서버 정보를 기준으로 종료하므로, 기존 브라우저에서 `http://localhost:3000`을 열거나 `kill 32264` 후 다시 `pnpm dev`/`npm run dev`를 실행하면 된다.

- 2026-06-15: 상품 상세 페이지 진입 시 선택된 card printing에 대해 eBay Buy Browse API 판매중 호가(`ebay_browse`)를 당일 1회 on-demand로 갱신하도록 연결했다. eBay/Supabase 서버 credential이 없거나 service role 권한이 맞지 않으면 기존 스냅샷 표시로 안전하게 fallback하고, 당일 `ebay_browse` snapshot이 이미 있으면 API를 재호출하지 않는다. eBay 호출은 3.5초 타임아웃을 둬 상세 렌더를 붙잡지 않게 했고, 검색어는 `card_printings.external_ids.name_en`/`name_ja`를 우선 사용하고 없을 때 한국어 카드명으로 fallback하도록 보강했다. Cron 가격 수집 후에는 `prices` cache tag를 즉시 revalidate해 새 snapshot이 UI에 반영되게 했다. 브라우저 검증 중 게스트 평점 조회가 `card_ratings` 권한 오류로 상세 Suspense를 깨뜨리는 현상을 확인해 permission denied를 미평가 상태로 fallback하도록 수정했다.

- 2026-06-13: 로컬 Supabase에는 카드 데이터가 복원됐지만 `/categories/pokemon`이 `0개 결과`로 보이던 문제를 확인했다. `.env.local`이 로컬 Supabase(`127.0.0.1:54321`)를 가리키고 REST API 직접 조회는 `cards=4677`로 정상임을 확인한 뒤, 오래된 Next dev 생성 산출물/캐시가 원인으로 판단해 기존 dev 서버를 종료하고 `.next`를 `/private/tmp/tcg-next-before-local-data-full-20260613-card-list-debug`로 이동한 뒤 `pnpm dev`를 재시작했다. 재검증 결과 `/categories/pokemon` HTML이 `포켓몬 4,677개 결과`와 실제 카드 목록을 렌더했다.

- 2026-06-13: 원격 Supabase 프로젝트 `fcaflidwlecbkbmtipjb`의 public 데이터를 로컬 Supabase DB로 복원했다. Supabase CLI를 원격 프로젝트에 link한 뒤 `supabase db dump --linked --data-only --schema public --exclude public.favorite_cards,public.card_ratings`로 `/private/tmp/tcground-remote-public-data.sql`을 생성했고, `supabase db reset` 후 로컬 Postgres 컨테이너에 복원했다. 자기참조 FK가 있는 `card_categories` 때문에 restore 세션에서만 `session_replication_role=replica`를 사용했다. 최종 로컬 row count는 `tcg_games=1`, `card_sets=42`, `cards=4677`, `card_printings=4677`, `card_categories=9`, `card_category_links=30`, `card_price_snapshots=321`, `price_observations=294`, `price_collection_runs=43`, `exchange_rates=1449`이며, 사용자 데이터 테이블 `favorite_cards`와 `card_ratings`는 제외해 0건으로 유지했다.

- 2026-06-13: 로컬 Supabase reset이 `202606030001_add_fx_price_display.sql`에서 `public.card_price_snapshots` 부재로 실패하던 문제를 수정했다. 원격 MCP migration으로만 존재하던 MVP/가격/평점 public schema를 `supabase/migrations/202605200001_create_local_baseline_schema.sql` baseline migration으로 복원하고, 설정된 seed 경로가 비어 있어도 reset이 진행되도록 `supabase/seed.sql` placeholder를 추가했다. 검증 결과 `pnpm dlx supabase start --exclude edge-runtime --ignore-health-check`와 `pnpm dlx supabase db reset` 모두 baseline → FX/display → category counts migration 순서로 통과했다. 일반 `supabase start`는 DB migration 통과 뒤 edge-runtime health check 502로 실패하므로 로컬 DB 검증 시 edge-runtime 제외 옵션을 사용한다.

- 2026-06-13: 카테고리 대분류 페이지(`/categories`) 최상위 래퍼 배경을 `bg-muted`에서 `bg-background`로 변경해 홈/인기/상세/세부 카테고리 페이지와 같은 전역 배경 토큰을 사용하도록 맞췄다. `pnpm lint`(기존 `packages/headless/dist` warning 7개), `pnpm exec tsc --noEmit`, `pnpm test --run`(284/284)이 통과했고, Playwright로 `/categories`와 `/cards`의 렌더된 body/header 배경색이 모두 `rgb(255, 248, 247)`이며 콘솔 에러가 없음을 확인했다.

- 2026-06-10: 프로젝트 README에 Docusaurus UI docs production 배포 사이트(`https://tcground-docs.vercel.app`)를 추가했다. 상단 라이브 링크, `apps/docs` 워크스페이스 설명, 문서 섹션, 배포 섹션에 UI docs URL을 반영했고, 해당 사이트가 서비스 문서가 아니라 `@tcground/ui` 컴포넌트 라이브러리의 설치, theme CSS, 컴포넌트 사용법, 접근성 기준을 다루는 UI 문서임을 명확히 설명했다. 실행 계획에는 6.10 README UI docs 링크 추가 항목으로 완료 처리했다. 변경 파일 Markdown Prettier check가 통과했다.

- 2026-06-10: `apps/docs` Docusaurus 문서 사이트를 Vercel 프로젝트 `tcground-docs`에 production 배포했다. `apps/docs/vercel.json`에 docs 전용 install/build/output 설정을 추가하고, `apps/docs/docusaurus.config.ts`의 URL을 `https://tcground-docs.vercel.app`로 갱신했다. `pnpm --filter @tcground/docs build`와 `pnpm dlx vercel build -A apps/docs/vercel.json --yes`가 통과했고, `pnpm dlx vercel deploy --prebuilt -A apps/docs/vercel.json --project tcground-docs --scope devjerryb-2567s-projects --yes`로 `dpl_3oTj6UARQ2i5UkppgngY8aW7umDM` 배포가 `Ready` 상태가 됐다. production URL `https://tcground-docs.vercel.app`와 `/installation`, `/components/button`, `/accessibility`가 HTTP 200으로 응답함을 확인했다. Vercel CLI가 GitHub 연결을 보고 기존 `tcg-round` 프로젝트에 자동 link할 수 있으므로, docs 배포 명령에는 `--project tcground-docs`를 명시하기로 했다. Vercel prebuilt 산출물 `.vercel/output/**`이 root lint 대상에 들어와 `pnpm lint`가 실패했으므로 ESLint global ignore에 `.vercel/**`도 추가했다.

- 2026-06-06: 카테고리/상세 스크롤 복원 조건을 보강했다. 저장된 목록 상태가 있어도 일반 카테고리 진입은 top에서 시작하도록 1회성 restore marker를 추가했고, 카테고리 카드 좌클릭으로 상세에 진입한 뒤 브라우저 뒤로가기를 했을 때만 목록/scrollY를 복원한다. 카드 상세 페이지에는 `CardDetailScrollReset`을 추가해 상세 진입 시 중간 스크롤에서 시작하는 현상을 막았다. Playwright 검증 결과 상세 진입 `scrollY=0`, 뒤로가기 후 목록 144개/6페이지와 `scrollY=6307` 복원, `/cards`→카테고리 일반 진입 `scrollY=0`을 확인했다. `pnpm lint`(기존 `packages/headless/dist` warning 7개), `pnpm exec tsc --noEmit`, `pnpm test --run`(284/284), 변경 파일 Prettier check가 통과했다.

- 2026-06-06: 카테고리 카드 목록에서 상세 페이지로 이동한 뒤 브라우저 뒤로가기를 했을 때 스크롤 위치가 유지되지 않는 문제를 수정했다. 모바일 무한 스크롤로 추가 로드한 카드 목록, loaded page, scrollY를 sessionStorage에 필터/정렬/보기 key별로 저장하고, 같은 카테고리 상태로 복귀하면 서버 1페이지 props가 저장 상태를 덮어쓰기 전에 목록과 스크롤을 복원한다. 복원 완료 전 IntersectionObserver가 2페이지를 중복 로드하는 race도 차단했다. 회귀 테스트 3개를 추가했고 `pnpm lint`(기존 `packages/headless/dist` warning 7개), `pnpm exec tsc --noEmit`, `pnpm test --run`(282/282), Playwright 모바일 실제 흐름 검증이 통과했다.

- 2026-06-05: Vercel main 배포 미반영 원인을 조사했다. 원격 `main` 최신 SHA는 PR #5 merge commit `96424b89618794f7256b73e0ede0834f4e9c4c8f`였지만, Vercel 최신 production 배포 `tcground-ojt264nfx-devjerryb-2567s-projects.vercel.app`의 build log는 `Branch: main, Commit: dfb2680`으로 이전 main 커밋을 다시 빌드한 기록이었다. 최신 production은 `Ready`이고 `tcground.vercel.app` alias도 붙어 있으나, 최신 main merge commit 자체의 Vercel deployment/check run은 없었다. `/private/tmp/tcg-round-main`에서 `origin/main` archive 기준 `pnpm install --filter=tcg-round... --frozen-lockfile`와 `pnpm build`를 재현해 main 소스 빌드는 통과함을 확인했다. 결론: 빌드 실패가 아니라 이전 커밋 redeploy/최신 main deployment 미생성이 원인이다.

- 2026-06-05: `@tcground/headless`/`@tcground/ui` npm 배포 후 루트 앱 갱신을 시도했지만 npm scope 권한 문제로 중단했다. registry 조회 결과 `@tcground/headless@0.1.1`과 `@tcground/ui@0.1.2`는 이미 존재했으나, 설치된 `@tcground/ui@0.1.2`는 `workspace:^` dependency와 extensionless ESM re-export를 포함한 이전 산출물이라 `node import('@tcground/ui')`가 실패했다. 같은 버전은 재배포할 수 없으므로 `@tcground/headless@0.1.2`/`@tcground/ui@0.1.3`으로 patch bump 후 publish를 시도했지만 `npm whoami`는 E401, `npm publish --access public`은 `PUT https://registry.npmjs.org/@tcground%2fheadless` E404로 실패했다. 루트 앱은 깨진 `@tcground/ui@0.1.2` 소비 상태로 두지 않고 `@tcground/ui@0.1.0` lockfile과 Vitest local source alias 상태로 복구했다. 후속 배포는 npm 로그인과 `@tcground` scope publish 권한 확보 후 재개한다.

- 2026-06-05: `@tcground/headless`/`@tcground/ui` npm 공개 배포 전 ESM 산출물 계약을 보강했다. `packages/headless/src`와 `packages/ui/src`의 런타임 상대 import/export specifier에 `.js` 확장자를 명시해 build 후 `dist/*.js`가 Node ESM에서 바로 로드되도록 했고, `@tcground/ui`의 `@tcground/headless` dependency를 publish 가능한 `^0.1.1` semver range로 전환했다. `pnpm build:ui`, `node import('./packages/headless/dist/index.js')`, `node import('./packages/ui/dist/index.js')`, `pnpm --filter @tcground/headless pack --dry-run`, `pnpm --filter @tcground/ui pack --dry-run`으로 산출물 계약을 확인했다. 루트 앱은 아직 published `@tcground/ui@0.1.0`을 설치하므로 Vitest의 임시 `@tcground/ui` source alias는 유지하고, `@tcground/headless@0.1.1`과 `@tcground/ui@0.1.2` 배포 및 root dependency 갱신 후 제거한다. 검증으로 `pnpm exec tsc --noEmit`, `pnpm lint`(기존 `packages/headless/dist` warning 7개), `pnpm test --run`(279/279), `pnpm build`가 통과했다.

- 2026-06-05: 루트 앱의 `@tcground/ui` npm 배포본 소비 검증 계약을 정리했다. `tsconfig.json`에서 `@tcground/ui` source alias를 제거해 앱 import가 설치된 `node_modules/@tcground/ui -> .pnpm/@tcground+ui@0.1.0...` package export를 보도록 했고, `@tcground/headless` source alias는 `packages/ui` 로컬 소스 타입체크와 docs 개발 검증을 위해 유지했다. 앱이 직접 import하지 않는 `class-variance-authority`, `clsx`, `cmdk`, `radix-ui`, `tailwind-merge`는 루트 dependency에서 제거했으며, `pnpm why` 결과 해당 패키지들은 `@tcground/ui` 또는 `shadcn`의 transitive dependency로만 남는다. `pnpm exec tsc --noEmit`과 `pnpm build`는 alias 제거 후 통과했다. `pnpm test --run`은 현재 published `@tcground/ui@0.1.0`의 extensionless ESM re-export를 Vitest가 해석하지 못해 실패했으므로, unit test runner는 `vitest.config.mts`에서 로컬 UI source alias를 사용하도록 분리했다. 실제 `@tcground/headless` npm publish, `@tcground/ui`의 headless semver dependency 전환, dist JS `.js` extension 보강은 npm 권한 확인 후 진행한다.

- 2026-06-05: 컴포넌트 구조 리팩토링 잔여 작업을 우선순위대로 정리했다. route 전용 server action을 import하던 `LoginForm`/`SignupForm`을 `components/tcg/auth`에서 `app/login/_components`와 `app/signup/_components`로 이동해 route private component 경계를 맞췄고, `components/tcg/auth`에는 전역 로그아웃 action만 남겼다. `/categories/[categoryId]/_components/CardResults.tsx`는 infinite scroll/container만 담당하게 하고, 카드 타일/empty state를 `CardResultCards.tsx`, pagination을 `CardResultsPagination.tsx`로 분리했다. UI 패키지 분리 후 사용처가 없어진 루트 `lib/utils.ts`를 제거했으며, `architecture.md`는 `@tcground/headless` 2-layer 구조와 auth form 위치를 현재 구현 기준으로 갱신했다. 검증으로 focused vitest 38개, `pnpm exec tsc --noEmit`, `pnpm lint`(기존 `packages/headless/dist` warning 7개), `pnpm test --run`(279/279), `pnpm build`가 통과했다. npm publish 권한이 필요한 `@tcground/headless` 공개 배포와 루트 UI dependency/tsconfig alias 정리는 후속으로 남겼다.

- 2026-06-05: KREAM matching report의 남은 미해소 상품을 현대 세트 우선으로 Supabase MCP 재대조했다. 기존 `manual_kream` CSV product id를 제외한 unresolved 160개 중 `M*`, `SV*`, `S10~S12`, `S6~S9` 후보 80개를 `card_printings`의 `set_code + collector_number + ko/KR` 기준으로 확인했고, 확정된 14개 상품 25건만 `memory-bank/price-source-validation.csv`에 `manual_kream` evidence로 추가했다. 추가 대상은 메가리자몽X ex SR(`901600`) 1건, 빛나 SR(`906558`) 1건, 로토무 ex SAR(`906562`) 1건, 메가리자몽X ex MA(`901601`) 5건, 메가팬텀 ex MA(`907006`) 1건, 성호의 메타그로스 ex SAR(`907019`) 1건, 메가개굴닌자 ex RR(`904504`) 2건, 제르네아스 R(`904524`) 2건, 메가드래캄 ex RR(`904542`) 3건, 로켓단의 니드킹 ex SAR(`802733`) 3건, 그란돈 AR(`804580`) 1건, 피카츄 S(`804212`) 2건, 릴리에의 삐삐 ex SR(`802911`) 1건, 기라티나 VSTAR UR(`82638`) 1건이다. 구조화 inbox는 `memory-bank/kream-scrape/kream-supabase-resolved-modern-inbox-20260605.json`에 저장했다. 검증 결과 CSV는 data row 308개, 27컬럼, malformed 0개, `manual_kream` sold 188개이며, inbox 재실행 dry-run은 `new rows: 0`, `skipped(dup): 25`, `scripts/collect-prices.ts --csv --dry-run`은 `parsed=294`, `resolved=294`, `snapshots=281`이다. `pnpm lint`(기존 `packages/headless/dist` warning 7개), `pnpm exec tsc --noEmit`, `pnpm test --run`(269/269)이 통과했다.

- 2026-06-05: KREAM 보류 상품을 Supabase `card_printings`와 세트코드/번호 기준으로 재대조해, 사용자 요청대로 메가개굴닌자 계열과 레쿠쟈 VMAX HR은 건너뛰고 13개 상품 24건을 `memory-bank/price-source-validation.csv`에 `manual_kream` evidence로 추가했다. 추가 대상은 명희의 격려 SAR(`907059`) 3건, 호브의 자시안 ex SAR(`802921`) 1건, 메가가디안 ex MUR(`944199`) 2건, 릴리에의 결심 SR(`802224`) 1건, 나옹 ex SAR(`907058`) 1건, 레시라무 ex SAR(`802602`) 4건, N의 레시라무 AR(`802906`) 3건, 민화의 초대 SR(`804748`) 2건, 모야모의 찌리배리 ex UR(`802923`) 1건, 세류 SR(`803783`) 1건, 치라미 AR(`802452`) 1건, 두빅굴 AR(`802411`) 1건, 투희 SAR(`802606`) 3건이다. 잉어킹 AR(`804967`) 4건은 기존 CSV 중복으로 제외됐다. 검증 결과 CSV는 data row 283개, 27컬럼, malformed 0개, `manual_kream` sold 163개이며, 구조화 inbox 재실행 dry-run은 `new rows: 0`, `skipped(dup): 28`, `scripts/collect-prices.ts --csv --dry-run`은 `parsed=269`, `resolved=269`, `snapshots=258`이다.

- 2026-06-05: 재로그인된 Playwright KREAM 세션에서 상위 30개 상세 상품을 직접 열어 체결 196건을 추가 확인했다. 이 중 Supabase `card_printings`와 확실히 매칭된 18개 상품 115건을 기존 CSV와 대조했고, 이미 들어간 58건은 중복으로 건너뛰고 57건을 `memory-bank/price-source-validation.csv`에 `manual_kream` evidence로 추가했다. 사용자 요청 예시였던 `802229` 릴리에의 결심 SAR 090/063 5건(PSA 10 280000원 2026-05-31, PSA 9 120000원 2026-05-28, BRG 10 영문 190000원 2026-05-16, PSA 9 119000원 2026-05-13, PSA 9 110000원 2026-05-05)을 포함한다. 최초 2건만 반영된 이유는 KREAM 모델번호를 기존 CSV에 이미 있던 sample만으로 보수 매칭했기 때문이며, 이번에는 Supabase 카탈로그를 조회해 `PKMKR-<card_num>` sample_id를 확정했다. `79041` 기라티나 V는 KREAM 모델번호 `S11111-100`을 Supabase `collector_number=111/100`, `external_ids.card_num=BS2022014110`으로 확인한 뒤 26건을 추가했다. 검증 결과 CSV는 data row 259개, 27컬럼, malformed 0개, sold 245개, `manual_kream` sold 139개이며 같은 입력 재실행 dry-run은 `new rows: 0`, `skipped(dup): 115`였다. 산출물은 `memory-bank/kream-scrape/kream-direct-results.json`과 `memory-bank/kream-scrape/kream-direct-matching-report.json`에 저장했다.

- 2026-06-05: 로그인된 Playwright KREAM 세션에서 `포켓몬카드 한글판` 검색 결과를 수동 evidence로 수집했다. 검색 결과 product link 2,045개 중 `거래 N`이 표시된 상품은 272개였고, 현재 DOM에 유지된 246개를 상세 수집 대상으로 처리했다. HTML 상세 수집으로 176개 상품의 체결 446건을 `memory-bank/kream-scrape/kream-product-details.json`에 저장했고, 기존 CSV 카탈로그와 확실히 매칭된 `803225` 이브이 ex SAR 223/187 PSA 10 130000원(2026-05-08), `804730` 파이리 AR 168/165 BRG 9 영문 50000원(2026-05-13) 2건을 `memory-bank/price-source-validation.csv`에 `manual_kream`으로 추가했다. 나머지 174개 상품 444건은 카탈로그 미등록/미매칭으로 `memory-bank/kream-scrape/kream-matching-report.json`과 `memory-bank/kream-worklist-inbox.md`에 보류했고, 70개 상품은 KREAM 상세 HTML이 기본 페이지를 반환하거나 HTTP 500/로그인 리다이렉트로 바뀌어 재로그인 후 재수집이 필요하다.

- 2026-06-05: eBay scrape offset 50~99 batch를 `node --env-file=.env.local --import tsx scripts/collect-prices.ts --ebay-scrape --offset 50 --limit 50 --source-batch-size 50`로 실제 실행했다. run은 `status=succeeded`, metadata batch(`cardStart=50`, `cardEnd=99`, `cardCount=50`)로 기록됐지만 observations/snapshots는 0건이었다. Supabase 확인 결과 `price_observations where source_name='ebay_scrape' = 0`, `card_price_snapshots where source_name='ebay_scrape' = 0`, 최신 run 2개(offset 0~49, 50~99)가 모두 succeeded/0이었다. 원인 점검 결과 eBay live HTML이 기존 `s-item` 대신 `s-card` search result markup을 쓰고, anti-bot browser verification page도 HTTP 200으로 내려와 기존 코드가 차단 HTML을 빈 결과처럼 처리할 수 있었다. `lib/pricing/ebay/scrape-adapter.ts`를 `s-card` title/price/link/sold date 구조와 한국식 날짜 fallback까지 파싱하도록 보강하고, eBay browser verification HTML은 `eBay sold scrape blocked by browser verification page` 오류로 기록하게 수정했다. 수정 후 `--ebay-scrape --dry-run --offset 50 --limit 2 --source-batch-size 2`는 verification page를 `failed`로 드러냈다. 검증으로 `pnpm exec vitest run lib/pricing/ebay/scrape-adapter.test.ts lib/pricing/collect-prices.test.ts`, `pnpm exec tsc --noEmit`, `pnpm lint`(기존 `packages/headless/dist` warning 7개), `pnpm test --run`(262/262)이 통과했다.

- 2026-06-05: eBay scrape 전체 실행 실패를 작은 단위로 재시도할 수 있도록 `scripts/collect-prices.ts`에 `--offset`, `--limit`, `--source-batch-size`를 추가하고, `collectDailyPrices`가 카드 window를 적용한 뒤 source batch마다 console progress와 `price_collection_runs` row를 남기도록 변경했다. browser context 종료 오류는 같은 닫힌 context로 남은 전체 카탈로그를 계속 처리하지 않고 해당 batch에서 abort하도록 막았다. 검증 결과 `--dry-run --ebay-scrape --offset 0 --limit 2 --source-batch-size 1`은 batch 2개가 succeeded였고, 실제 `--ebay-scrape --offset 0 --limit 50 --source-batch-size 50`은 run metadata(`cardStart=0`, `cardEnd=49`, `sourceBatchSize=50`)를 남긴 succeeded run으로 완료됐다. 첫 50장에서는 eBay sold 매칭이 없어 Supabase count는 `ebay_scrape` observation 0, snapshot 0이다. 검증으로 `pnpm exec vitest run lib/pricing/collect-prices.test.ts`, `pnpm exec tsc --noEmit`, `pnpm lint`(기존 `packages/headless/dist` warning 7개), `pnpm test --run`(260/260)이 통과했다.

- 2026-06-05: production eBay Browse 전환 없이 가격 쓰기 수집을 실행했다. 제한 dry-run에서 KREAM은 5/5 `KREAM search failed (500)`라 제외하고, `--bunjang --ebay-scrape` 실제 쓰기를 실행했다. Bunjang은 succeeded로 26개 asking snapshot을 생성했으며, eBay scrape는 브라우저 context 종료로 partial run(0 observations, 0 snapshots)만 기록됐다. 이어서 `--csv --csv-asking --fx`를 실제 재적재해 sold 107개/asking 5개 snapshot을 upsert했고, 중복 sold observation은 0건 insert였다. Supabase 최종 count는 `card_price_snapshots=138`, `price_observations=109`, `price_collection_runs=4`, `exchange_rates=1449`; source별 snapshot은 `pricecharting_ebay_sold=64`, `ebay_sold=30`, `bunjang=26`, `manual_bunjang=10`, `manual_joongna=5`, `manual_kream=3`. 검증으로 `pnpm exec tsc --noEmit`와 `pnpm exec vitest run lib/pricing/collect-prices.test.ts lib/pricing/aggregate.test.ts lib/pricing/csv-import.test.ts`가 통과했다.

- 2026-06-05: 가격 수집/CSV canonicalization/판본 선택 변경 묶음의 검증을 완료했다. 핵심 테스트 72개, 전체 `pnpm exec tsc --noEmit`, `pnpm lint`(기존 `packages/headless/dist` warning 7개), `pnpm test --run`(255/255), `pnpm build`가 통과했다. `scripts/collect-prices.ts --csv --csv-asking --dry-run`은 sold `109/109/107`, asking `9/9/5`로 통과했고, `--browse --dry-run --limit 5`는 `cardsProcessed=5`, `status=succeeded`, sandbox라 snapshot 0개였다. `next dev -p 3003`에서 `/categories/pokemon`과 `/cards/sv2a-bs2023014201-리자몽-ex`가 실제 가격/표본/판본 선택/차트 영역을 렌더하고 콘솔 에러가 없음을 확인했다.

- 2026-06-04: `--csv` 적재 검증 중 발견한 중복 카드/레거시 snapshot을 정리했다. KR-003~010은 카탈로그 트윈으로 통합하고 seed 카드/printing 8장을 삭제했으며, 레거시 `source_name='aggregate'` snapshot 107행을 제거했다. 통합 후 `getSampleIdToPrintingId`가 Supabase 기본 1000행만 읽어 일부 CSV row가 미해소되던 문제를 페이지네이션으로 수정해 `resolved=109/snapshots=107`, asking 9/9/5를 회복했다.
- 2026-06-04: 한국판 전체 가격 수집 실행 런북을 `implementation-plan.md` 3.5에 확정했다. 수집은 한국 IP 로컬 실행을 기준으로 하고, 범위는 우선 카드 sold evidence와 자동 전체 asking 2트랙으로 둔다.
- 2026-06-04: 우선 카드(`KR-001`~`KR-060`) 중 raw sold < 3인 28장을 자동 sold 수집 우선 대상으로 확정했다: `KR-020`, `KR-028`, `KR-029`, `KR-032`~`KR-035`, `KR-038`, `KR-040`~`KR-060`.

- 2026-06-05: 한국판 전체 가격 수집 런북(3.5)의 제한 dry-run 검증을 완료했다. 전체 dry-run은 브라우저 source까지 켜면 중간 출력 없이 장시간 실행되어 운영 검증이 불투명했으므로 `scripts/collect-prices.ts --limit N` 옵션을 추가하고 `collectDailyPrices`에 `cardLimit`을 연결했다. 이 과정에서 브라우저 fetch가 eBay Browse/Guardian/Bunjang 같은 비브라우저 source에도 주입되어 eBay OAuth가 `unsupported_grant_type`로 실패하던 버그를 발견해, 브라우저 fetch는 KREAM/eBay scrape에만 전달하도록 수정했다. 검증 결과 `--browse --dry-run --limit 5`는 성공했지만 `EBAY_ENV=sandbox`라 snapshot 0개였고, 전체 활성 source `--dry-run --limit 2`는 Browse/Bunjang/eBay scrape 성공, Guardian 404, KREAM 500으로 `partial`이었다. Supabase row count는 dry-run 후 `card_price_snapshots=112`, `price_observations=109`, `price_collection_runs=2`, `exchange_rates=1449`로 유지됐다. 실제 전체 쓰기는 production eBay keyset 전환과 실패 source 플래그 정리 후 진행한다.

- 2026-06-05: `KOREA_EXIM_FX_API_KEY` 설정 후 `scripts/collect-prices.ts --csv --csv-asking --fx` 재적재를 완료했다. dry-run은 FX fetch와 CSV 해소 수치(sold 109/109/107, asking 9/9/5)가 정상이었다. 실제 실행 중 ① 휴일/주말 fallback으로 같은 `rate_date`가 여러 요청에서 반복돼 `exchange_rates` upsert batch가 중복 conflict를 일으키는 문제, ② 새 `price_observations` unique index 기준과 기존 중복 필터가 완전히 일치하지 않아 manual KREAM observation insert가 중복 conflict를 일으키는 문제를 확인했다. `upsertExchangeRates`에 conflict key dedupe를 추가하고, observation identity를 source item/url 양쪽 + 정규화된 `sold_at`/`sold_price` bucket으로 맞춘 뒤 회귀 테스트를 추가했다. 최종 실제 실행 결과 exchange-rate row 1,449개 upsert, sold observation 2건 insert, sold snapshot 107개 upsert, asking snapshot 5개 upsert. Supabase 검증 결과 `exchange_rates` 1,449행/63개 rate date, `card_price_snapshots` 112개 전부 display 가격 보유, FX 환산 snapshot 99개(USD 97, GBP 2), source별 observation은 `manual_kream` 3건 포함 총 109건이다.

- 2026-06-05: Supabase MCP migration `add_fx_price_display`로 원격 DB에 FX/display 가격 확장을 적용했다. 확인 결과 migration 기록은 `20260604155747`, `card_price_snapshots`의 `source_*`/`display_*`/`fx_*` 컬럼과 `exchange_rates` 테이블, `price_observations_source_item_bucket_unique_idx`/`price_observations_source_url_bucket_unique_idx`가 생성됐고 기존 snapshot 112개 전부 `source_currency`/`display_currency` backfill이 완료됐다. `scripts/collect-prices.ts --csv --csv-asking --dry-run`은 sold `parsed=109/resolved=109/snapshots=107`, asking `parsed=9/resolved=9/snapshots=5`로 통과했다. `--fx --dry-run`은 `.env.local`에 `KOREA_EXIM_FX_API_KEY`가 없어 `Korea Eximbank FX import requires KOREA_EXIM_FX_API_KEY`에서 중단되므로, KRW display snapshot 재적재와 manual KREAM grade별 관측치 2건 보강은 키 설정 후 진행한다. Supabase Advisor 확인 결과 이번 작업 관련 신규 blocking 이슈는 없고, 새 display index는 적용 직후라 unused INFO로 표시된다.

- 2026-06-04: 가격 기준 재정립에 맞춰 수동 CSV sold snapshot의 source 보존과 sold/asking 분류를 정리했다. `scripts/collect-prices.ts --csv`는 sold 관측치를 `ebay_sold`, `pricecharting_ebay_sold`, `manual_kream`, `manual_bunjang` 등 원천별로 따로 집계해 `card_price_snapshots.source_name`에 보존한다. 상세 차트는 `source_name`만 보지 않고 `aggregation_method`를 우선해 `manual_asking_median`/`*_asking_median`은 asking trend, `median_filtered`는 sold series로 분류하므로 같은 `manual_bunjang`라도 판매중 호가와 판매완료 evidence가 섞이지 않는다. 가격 요약 source label도 `PriceCharting eBay sold`, `KREAM`, `번개장터` 같은 source명을 드러내도록 보강했다. `pricecharting_ebay_sold` CSV 파서 테스트와 source별 sold 집계 테스트를 추가했다. `node --env-file=.env.local --import tsx scripts/collect-prices.ts --csv --csv-asking --dry-run` 결과 `parsed=109/resolved=109/snapshots=107`, asking `parsed=9/resolved=9/snapshots=5`. `pnpm exec vitest run lib/pricing/aggregate.test.ts lib/pricing/csv-import.test.ts lib/tcg-catalog.test.ts`, `pnpm exec tsc --noEmit`, `pnpm lint`(기존 `packages/headless/dist` warning 7개만), `pnpm test --run`(253/253), `pnpm build` 통과.

- 2026-06-04: 가격 source validation CSV의 `sample_id`를 공식 한국 카드 번호 기반 `PKMKR-<card_num>` 체계로 통일했다. 기존 `KR-001`~`KR-060` evidence/pending 행 152개는 `PKMKR-*`로 변환하고 legacy worklist 번호를 `raw_payload_json.worklist_id`에 보존했다. 실제 evidence가 없고 세트/번호/희귀도 잠정값이던 `KR-061`~`KR-110` pending skeleton 50개는 제거해 전체 카탈로그 `PKMKR-*` backlog로만 추적하게 했다. `lib/tcg-catalog.ts`는 `card_num`/공식 `BS...` set code를 우선해 `PKMKR-*`를 표시하고, CSV resolver도 `set_code` fallback으로 `PKMKR-BS2016009103/104` 같은 legacy seed를 해소한다. CSV 검증 결과 data row 3,344개, malformed row 0개, `KR-*` sample id 0개. `node --env-file=.env.local --import tsx scripts/collect-prices.ts --csv --dry-run` 결과 `parsed=109`, `resolved=109`, `snapshots=107`, `dryRun=true`. `pnpm exec vitest run lib/pricing/csv-import.test.ts lib/tcg-catalog.test.ts app/cards/page.test.tsx 'app/cards/[cardId]/page.test.tsx'`, `pnpm lint`(기존 `packages/headless/dist` warning 7개만), `pnpm exec tsc --noEmit`, `pnpm test --run`(249/249), `pnpm build` 통과.

- 2026-06-04: 상품 상세 가격 변동 chip의 `trending_up`/`trending_down`/`trending_flat` material icon fallback을 제거하고 `lucide-react`의 `TrendingUp`, `TrendingDown`, `Minus`로 교체했다. 아이콘 폰트 로딩에 의존하지 않으므로 상승/하락/보합 표시가 안정적으로 렌더된다. `pnpm exec tsc --noEmit`, `pnpm exec vitest run 'app/cards/[cardId]/page.test.tsx'` 통과.

- 2026-06-04: 카드 목록/상세의 기본 판본을 한국판(`ko/KR`)으로 고정하고, 상품 상세에서 한국판/일본판/미국판 선택 UI를 추가했다. `edition=kr|jp|na` 쿼리로 선택 상태를 유지하며 기본은 한국판이다. 선택한 `card_printing_id` 기준으로 이미지, 가격 요약, 가격 차트를 조회하고, 가격 히스토리 bucket key에 `market`을 포함해 KR/JP/NA 시장 가격이 섞이지 않게 했다. 목록/인기 카드는 한국 포켓몬센터 CDN 이미지가 있으면 일본판 TCGdex thumbnail보다 우선한다. Supabase MCP로 기존 일본판 이미지 URL이 남아 있던 8개 seed 카드(`KR-003`~`KR-010` 중 8장)의 `cards.image_url`, `cards.thumbnail_url`, 한국판 `card_printings.image_url`을 `cards.image.pokemonkorea.co.kr` URL로 갱신했고 row count 8/8을 검증했다. `pnpm exec vitest run lib/tcg-catalog.test.ts 'app/cards/[cardId]/page.test.tsx'`, `pnpm exec tsc --noEmit`, `pnpm lint`(기존 `packages/headless/dist` warning 7개만), `pnpm test --run`(249/249), `pnpm build` 통과.

- 2026-06-03: `/categories/pokemon` 페이지 이동 성능 병목을 수정했다. `generateMetadata`가 `getPokemonCategoryPageData()`로 전체 카테고리 데이터를 다시 읽던 중복 조회를 제거했고, 기본 `추천순(best)`은 전체 포켓몬 카드와 전체 primary printing snapshot을 읽어 서버에서 정렬하지 않도록 변경했다. 이제 `card_price_snapshots`의 가격 후보 printing을 먼저 읽어 가격 데이터가 있는 카드 ID 순서를 만들고, 현재 페이지에 필요한 카드 row와 snapshot만 조회하며, 부족분은 가격 후보를 제외한 slug 순 fallback으로 채운다. 측정 결과 warm request 기준 `/categories/pokemon`은 기존 약 11.4초에서 약 1.9초, 추가 로그에서는 657ms~985ms 수준까지 개선됐다. `pnpm exec vitest run lib/tcg-catalog.test.ts 'app/categories/[categoryId]/page.test.tsx'`, `pnpm exec tsc --noEmit`, `pnpm lint`(기존 `packages/headless/dist` warning 7개만), `pnpm test --run`(242/242), `pnpm build` 통과. 브라우저 MCP는 기존 프로필 잠금으로 직접 스냅샷을 못 떴지만, Next dev server 로그에서 `/categories/pokemon` 200 렌더와 browser LCP/scroll warning만 확인했다.

- 2026-06-03: `/categories/pokemon` 기본 카드 리스트의 `추천순`을 가격 데이터 우선 정렬로 변경했다. `best` 정렬은 후보 카드를 가격 요약이 있는 카드 우선으로 정렬하고, 가격 요약이 있는 카드끼리는 최신 표시 가격의 `sampleCount`가 많은 순으로 정렬한다. 가격 데이터가 없거나 표본 수가 같은 카드는 기존 slug 순서를 유지한다. 명시적 이름 정렬(`name-asc`, `name-desc`)은 기존 DB 정렬/페이지네이션 동작을 유지한다. 전체 후보/프린팅 snapshot 조회는 Supabase/Cloudflare URL 한도를 넘지 않도록 500개 카드, 100개 printing 단위 chunk로 나눠 읽는다. `memory-bank/prd/category.md`, `memory-bank/prd/search-results.md`에 기본 추천순 기준을 반영했고, Playwright로 `http://localhost:3000/categories/pokemon` 200 렌더와 첫 카드 표본 수 정렬(4건 → 3건 → 2건)을 확인했다. `pnpm exec vitest run lib/tcg-catalog.test.ts`, `pnpm exec tsc --noEmit`, `pnpm lint`(기존 `packages/headless/dist` warning 7개만), `pnpm test --run`(242/242), `pnpm build` 통과.

- 2026-06-03: FX/display migration 미적용 원격 DB에서 앱의 snapshot 조회가 `column card_price_snapshots.source_currency does not exist`로 실패하던 문제를 수정했다. 기존 fallback은 `schema cache` 문구가 있는 오류만 감지했으나, PostgREST가 일반 `column ... does not exist` 문구로 반환하는 경우도 있어 `lib/tcg-catalog.ts`의 missing FX/display column 감지 조건을 넓히고 회귀 테스트를 추가했다. `pnpm exec vitest run lib/tcg-catalog.test.ts`, `pnpm exec tsc --noEmit`, `pnpm lint`(기존 `packages/headless/dist` warning 7개만), `pnpm test --run`(240/240) 통과.

- 2026-06-03: `memory-bank/price-source-validation.csv`의 검증된 sold/asking 행을 원격 Supabase snapshot에 재적재했다. dry-run 결과 sold `parsed=109`, `resolved=109`, `snapshots=107`, asking `parsed=9`, `resolved=9`, `snapshots=5`였고, 실제 실행 결과 `price_observations` sold 66건을 추가 insert하고 `card_price_snapshots`에 sold snapshot 107건, asking snapshot 5건을 upsert했다. `source_name=pending`, `exclude_reason=pending_evidence` 행은 파서에서 제외되어 적재되지 않았다.

- 2026-06-03: 전체 한국판 포켓몬 카탈로그를 가격 source 검증 worklist로 확장했다. Supabase `card_printings` 기준 한국판 프린팅 3,668개 중 기존 CSV에서 이미 커버된 476개를 제외하고, `memory-bank/price-source-validation.csv`에 `PKMKR-<card_num>` sample id의 `pending_evidence` 행 3,192개를 추가했다. 이 행들은 `source_name=pending`, `exclude_reason=pending_evidence`라 파서/집계가 공개 가격으로 사용하지 않는다. 후속 evidence 입력이 DB row를 직접 수정하지 않고도 해소되도록 `lib/pricing/collect-prices.ts`의 sample id resolver가 `external_ids.card_num`을 `PKMKR-<card_num>`으로도 매핑하게 했다. 자동 동기화 스크립트 `scripts/sync-price-worklist.ts`를 추가했으며, CSV 구조 검증 결과 총 data row 3,394개, 27컬럼, malformed row 0개, `PKMKR-*` 중복 0개. `pnpm exec vitest run lib/pricing/csv-import.test.ts`, `pnpm exec tsc --noEmit`, `pnpm lint`(기존 `packages/headless/dist` warning 7개만), `pnpm test --run`(239/239) 통과.

- 2026-06-03: `memory-bank/price-source-validation.csv`에 PriceCharting 개별 eBay completed-sale evidence 12건을 추가했다. `KR-036` 피카츄 ex 132/106 SAR, `KR-037` 피카츄 ex 136/106 UR, `KR-039` 갸라도스 ex 091/078 SR은 카드당 raw sold 3건을 충족해 pending placeholder를 제거했다. `KR-020`, `KR-029`, `KR-038`은 raw sold 1건씩만 확보되어 pending을 유지한다. CSV 구조 검증 결과 총 202행, malformed row 없음. `pnpm exec vitest run lib/pricing/csv-import.test.ts`, `pnpm lint`(기존 `packages/headless/dist` warning 7개만), `pnpm exec tsc --noEmit`, `pnpm test --run`(239/239) 통과.

- 2026-06-03: `KR-031`처럼 실제 `pricecharting_ebay_sold` evidence 3건과 `pending_evidence` placeholder가 동시에 남아 헷갈리던 CSV 상태를 정리했다. `KR-011`~`KR-019`, `KR-021`~`KR-027`, `KR-030`, `KR-031`은 이미 raw sold 표본 3건을 충족하므로 `memory-bank/price-source-validation.csv`에서 pending placeholder를 제거했고, pending은 아직 증거가 부족한 `KR-020`, `KR-028`, `KR-029`, `KR-032`~`KR-110`에만 남기도록 PRD/실행 계획/trouble-shooting 문구를 동기화했다.

- 2026-06-03: `KR-011`~`KR-060` 50장 실제 sold 표본 보강 1차 pass를 진행했다. `memory-bank/price-source-validation.csv`에 `pricecharting_ebay_sold` 54건을 추가해 `KR-011`~`KR-019`, `KR-021`~`KR-027`, `KR-030`, `KR-031` 총 18장이 카드당 raw sold 3건을 충족했다. 사용한 source는 PriceCharting historic sales 표의 개별 eBay completed-sale 행이며, 직접 eBay 원문 접근이 현재 환경에서 차단되므로 confidence를 보수적으로 두고 `source_name=pricecharting_ebay_sold`로 분리했다. CSV 구조 검증 결과 총 212행, 새 source 54행, malformed row 없음. `pnpm exec vitest run lib/pricing/csv-import.test.ts` 통과.
- 2026-06-03: priority 카드 50장 추가 요청을 `KR-001`~`KR-110` worklist 상태로 마감 정리했다. `memory-bank/price-source-validation.csv`는 unique sample id 110개를 가지며, `KR-011`~`KR-060` 50개와 `KR-061`~`KR-110` 50개가 모두 `source_name=pending`, `exclude_reason=pending_evidence`, `price_kind=sold`, `variant=raw` 스켈레톤으로 검증됐다. `implementation-plan.md`의 수동 sold CSV 보강 workflow/checklist를 완료 처리하고, 제품 PRD와 trouble-shooting에 pending 후보가 실제 가격 표본이 아니라 공개 snapshot에서 제외되는 검증 대기 행임을 명시했다.
- 2026-06-03: 4.20 가격 데이터 적재 1차를 완료했다. `supabase/migrations/202606030001_add_fx_price_display.sql`에 `exchange_rates`, snapshot source/display 가격·환율 기준일 컬럼, manual evidence source의 product id 중복을 허용하는 `price_observations` unique index 교체를 정의했고, `lib/pricing/fx.ts`로 한국수출입은행 환율 OpenAPI fetch/import 및 KRW display 환산을 구현했다. `scripts/collect-prices.ts --csv --csv-asking`를 실제 실행해 현재 원격 legacy schema 기준 수동 CSV sold 관측치 41건을 `price_observations`에 insert하고, sold snapshot 43개와 asking snapshot 6개를 upsert했다. 원격 DB에는 아직 FX/display migration이 적용되지 않아 snapshot은 legacy 컬럼으로 적재됐고, 기존 `source_item_id` unique index 때문에 manual KREAM grade별 관측치 2건은 migration 적용 후 `--fx` 재적재로 보강해야 한다. `pnpm lint`(기존 `packages/headless/dist` warning 7개만), `pnpm exec tsc --noEmit`, `pnpm test --run`(239/239), `pnpm build` 통과.
- 2026-06-03: 결론은 한국판 카탈로그 약 3,600개는 충분하므로 증설하지 않고, P0 가격 전략을 eBay Browse asking daily snapshot + 검증 가능한 수동 sold CSV + 기준일 FX 환산으로 둔다. Marketplace Insights sold 자동화와 KREAM/번개장터/중고나라 자동 수집은 접근 승인/재사용 권한 확인 전까지 보류한다. 3,600개 전체 수동 sold 데이터 생성은 목표에서 제외하고 priority 카드 50~100개부터 evidence 기반으로 보강한다.
- 2026-06-02: 시세 수집을 신원 확정 기반으로 재설계했다. 네이버 쇼핑(`naver_shopping`)은 관련성 검증 없이 액세서리·다른 버전을 한 버킷에 섞어 `블래키 ex` raw가 `min 200 / median 6,105 / max 1,349,300`으로 깨져 있었다 → 소스 삭제 + DB 행 5,231개 제거. 공유 `match-confidence`로 리스팅 제목을 카드명(영/일/한)·번호·세트와 대조해 confidence 점수화하고, 집계(`aggregate.ts`)의 `<0.70`+IQR로 오염을 떨군다. eBay scrape는 `_sacat`(CCG Individual Cards) 카테고리 한정 + 리스팅별 점수, KREAM은 한글명 검색→`resolveKreamProduct` 최고매칭 체결가로 전환했다. 영문/일문명은 `scripts/enrich-card-names.ts`가 TCGdex에서 `external_ids.name_en/name_ja`로 보강한다(JP 전용 세트는 영문 null). 데이터센터 IP에서 eBay 403·KREAM 500 전면 차단을 확인해, 두 소스는 Playwright 브라우저 페처(`lib/pricing/browser/browser-fetch.ts`)로 로컬/한국 IP `scripts/collect-prices.ts`에서만 수집하고 Vercel Cron(fetch 미주입)에선 자동 제외한다. 리스트/홈/`/cards`의 `formatKrw` 하드코딩(USD가 ₩로 오표시)을 `formatPrice(value, currency)`로 고쳤다. `tsc --noEmit`·vitest 234개·eslint 통과. 자세한 경위는 `trouble-shooting.md` 참조.
- 2026-06-02: `/categories` 대분류 카드 수가 1,000개로 잘려 보이던 문제를 수정했다. 원인은 `getTcgCategoryOverview`가 Supabase `select('id, game_id')`로 받은 row 배열 길이를 세고 있었고, PostgREST 응답이 기본 1,000행까지만 내려와 실제 포켓몬 카드 수 3,668개를 반영하지 못한 것이다. `cards`/`card_sets`는 게임별 `select('id', { count: 'exact', head: true })` count 쿼리 결과를 우선 사용하도록 바꿨고, row limit 회귀 방지 테스트를 추가했다. 카테고리 타일 화살표 SVG도 `fill='currentColor'`로 바꿔 부모 `text-white`를 따르게 했다. `pnpm exec tsc --noEmit`, 관련 테스트 26개, `pnpm lint`(기존 `packages/headless/dist` warning 7개만), `pnpm build` 통과.

- 2026-06-02: `/categories` 대분류 목록을 fake `tcgCategories`에서 Supabase 카탈로그 집계 기반으로 전환했다. `lib/tcg-catalog.ts`에 `getTcgCategoryOverview`/`mapTcgCategoryOverviewRows`를 추가해 `tcg_games`, `cards`, `card_sets`, `card_printings`, `card_price_snapshots`를 읽고, 포켓몬/유희왕/원피스/매직 더 개더링 기본 대분류는 항상 노출하도록 했다. Supabase에 없는 기본 대분류는 카드/세트/가격 기록을 0으로 표시하며, 기존 fake 숫자는 `lib/tcg-data.ts`에서 제거했다. `/categories` UI는 관련 이미지 배경 타일로 재설계했고, 포켓몬/유희왕/매직은 기존 홈 카테고리 이미지를 재사용, 원피스는 사용 가능한 이미지가 없어 임시 해상 visual fallback을 쓴다. `pnpm exec tsc --noEmit`, `pnpm lint`(기존 `packages/headless/dist` warning 7개만), `pnpm test --run`(224/224), `pnpm build`를 통과했고 `http://localhost:3000/categories`에서 4개 대분류와 0 값, console warning/error 없음 확인.

- 2026-06-01: 국내 시세 source로 KREAM(체결 시세=sold)과 번개장터(판매중 호가=asking)를 추가했다. 둘 다 공식 공개 API와 재사용 권한이 없어, eBay Marketplace Insights와 동일하게 **스캐폴드 + 수동 import** 방식으로 결정했다(비공식 엔드포인트 스크래핑은 하지 않음). `lib/pricing/kream/`(`mapKreamTradesToObservations`, 옵션 라벨→grade/variant 파싱), `lib/pricing/bunjang/`(`mapBunjangListingsToSnapshot`, median asking)에 매핑·테스트를 두고 live 호출은 `KREAM_COLLECTION_ENABLED`/`BUNJANG_COLLECTION_ENABLED` 플래그로 `PriceSourceAccessNotGrantedError`를 던져 막았다(일반화한 에러를 `price-source.types.ts`에 추가하고 `EbayAccessNotGrantedError`는 호환 별칭으로 유지). KREAM sold(`manual_kream`)는 기존 `parsePriceValidationCsv`→`aggregateObservations` 경로를 그대로 쓰고, 번개 asking(`manual_bunjang`)용으로 `parseAskingValidationCsv`/`aggregateAskingObservations`와 `scripts/collect-prices.ts --csv-asking`를 추가했다. 차트는 `isAskingSource`로 asking(eBay Browse·번개)/sold(KREAM 등)를 분류하고 동률 시 KR/KRW 버킷을 우선하도록 `buildPriceHistory`를 일반화했으며, 출처 라벨을 통화별로 보정했다. DB 스키마 변경은 없다(`source_name`만 추가). `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test --run`, `pnpm build`를 통과했다.

- 2026-05-29: Radix UI 같은 직접 구현 접근성 primitive 라이브러리 방향에 맞춰 `@tcground/ui`의 대표 컴포넌트 3개(Button, Tabs, Dialog)를 Radix primitive 의존 없이 자체 구현으로 전환했다. `packages/ui/src/components/ui/primitive.tsx`를 추가해 ref/event/`asChild` prop 병합을 공유하고, Button은 Radix `Slot` 없이 `asChild` disabled semantics(`aria-disabled`, `tabIndex=-1`, click 차단)를 보장한다. Tabs는 자체 context로 `role=tablist/tab/tabpanel`, `aria-selected`, `aria-controls`, roving tabindex, 방향키/Home/End 이동을 구현했다. Dialog는 자체 portal, body sibling `aria-hidden`, `role=dialog`, title/description 연결, focus trap, Escape/overlay close, focus restore를 구현했다. Button/Tabs/Dialog 테스트를 추가·갱신하고 Dialog/Tabs 문서와 `headless-ui` PRD, architecture를 직접 primitive 기준으로 갱신했다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`(98/98), `pnpm build:ui`, `pnpm build:docs`를 통과했다. Docusaurus update check는 `/Users/songjihyeon/.config` 권한 때문에 경고를 냈지만 정적 빌드는 성공했다.

- 2026-05-29: eBay 가격 수집 어댑터 계약과 카드 상세 일주일 가격 추적 차트(`implementation-plan.md` 4.15)를 구현했다. 개인 개발자는 eBay Marketplace Insights(sold)가 Limited Release라 사실상 승인이 어려운 점을 확인하고, 일별 시계열은 개인도 가능한 Browse API(판매중 호가) 일일 수집으로 누적하고 수동 CSV sold 실거래가를 차트 참조점으로 오버레이하는 방향으로 결정했다. `lib/pricing/`에 source-agnostic 계약(`price-source.types.ts`), 집계(`aggregate.ts`), CSV import(`csv-import.ts`), eBay OAuth/Browse/Marketplace Insights scaffold(`ebay/`), 일일 수집 오케스트레이션(`collect-prices.ts`)을 추가하고, service-role 쓰기 클라이언트(`lib/supabase/admin.ts`)와 Vercel Cron route(`app/api/cron/collect-prices/route.ts`, `CRON_SECRET` 검증), `vercel.json` cron을 붙였다. Marketplace Insights 어댑터는 매핑/scaffold만 두고 `EbayAccessNotGrantedError`로 막았다. 카드 상세(`app/cards/[cardId]/page.tsx`)의 정적 SVG 차트를 `card_price_snapshots` 시계열(asking 추세선 + sold 오버레이 + 빈 상태)로 교체하고 통화별 표시(`formatPrice`)로 바꿨다. 데이터 최소화 정책(seller/user 미저장)과 env 키(`.env.example`)를 정리했다. 네트워크 의존은 모두 모킹했고 `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test --run`(92/92), `pnpm build`를 통과했다.

- 2026-05-29: `memory-bank/implementation-plan.md` 4.14 PR3 범위인 Docusaurus 오버레이/복합 인터랙션 컴포넌트 5개(`alert-dialog`, `popover`, `sheet`, `tooltip`, `command`) 문서를 작성했다. 각 컴포넌트별 MDX와 preview example, example `index.ts` export를 추가하고 `apps/docs/sidebars.ts`에 등록했으며, Tailwind 생성 없이 preview가 보이도록 `packages/ui/src/theme.css`에 AlertDialog/Popover/Sheet/Tooltip/Command의 `data-slot` fallback 스타일을 보강했다. `pnpm build:docs`, `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test --run`(63/63), `pnpm build`를 통과했다. `http://127.0.0.1:3013/components/alert-dialog/`와 `/components/command/`에서 새 사이드바 항목, preview 렌더, Alert Dialog open 동작을 확인했다. Docusaurus update check는 `/Users/songjihyeon/.config` 권한 때문에 경고를 냈지만 정적 빌드는 성공했다.
- 2026-05-28: `memory-bank/implementation-plan.md` 4.14 PR2 범위인 Docusaurus 피드백/표시 컴포넌트 7개(`alert`, `badge`, `card`, `avatar`, `separator`, `skeleton`, `table`) 문서를 작성했다. 각 컴포넌트별 MDX와 preview example, example `index.ts` export를 추가하고 `apps/docs/sidebars.ts`에 등록했으며, Tailwind 생성 없이 preview가 보이도록 `packages/ui/src/theme.css`에 해당 `data-slot` fallback 스타일을 보강했다. `pnpm build:docs`, `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test --run`(63/63), `pnpm build`를 통과했고, `apps/docs/build` 산출물에서 새 문서 라우트와 사이드바 항목 생성을 확인했다. 인앱 브라우저와 Playwright 브라우저 세션은 현재 환경의 브라우저 점유 상태 때문에 직접 화면 캡처까지 진행하지 못했다.
- 2026-05-28: `apps/docs`의 `@tcground/ui` 컴포넌트 문서 확장 계획을 `implementation-plan.md` 4.14에 추가하고, PR1 범위인 폼/입력 계열 7개(`input`, `textarea`, `label`, `checkbox`, `radio-group`, `select`, `input-group`) 문서를 작성했다. 각 컴포넌트별 Docusaurus MDX와 preview example을 추가하고, `apps/docs/sidebars.ts`에 등록했으며, `packages/ui/src/theme.css`에 Input/Label/Checkbox/RadioGroup/Select/InputGroup/Textarea의 `data-slot` fallback 스타일을 보강했다. `pnpm build:docs`, `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test --run`(63/63), `pnpm build`를 통과했고, `http://127.0.0.1:3010/components/select/`에서 사이드바와 Select preview trigger/content 렌더를 확인했다.
- 2026-05-28: 루트 `tcground` 앱이 npm registry에 배포된 `@tcground/ui@0.1.0`를 실제 소비하도록 전환했다. 루트 `package.json`의 dependency를 `workspace:*`에서 `^0.1.0`으로 바꾸고 `pnpm install`로 `pnpm-lock.yaml`을 registry tarball 기준으로 갱신했다. `apps/docs`는 UI 패키지 문서/개발 검증용이므로 `workspace:*`를 유지한다. `node_modules/@tcground/ui`가 `.pnpm/@tcground+ui@0.1.0...`를 가리키는 것을 확인했고, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`(63/63), `pnpm build`를 통과했다.
- 2026-05-28: `@tcground/ui` npm 공개 배포 준비를 완료했다. `packages/ui/package.json`을 `private: false`, public `publishConfig`, GitHub repository/bugs/homepage metadata, MIT license, `dist/theme.css` export 기준으로 정리했고, `prepack`에서 build가 실행되도록 했다. build는 stale `dist`와 `.tsbuildinfo`를 먼저 지운 뒤 TypeScript 산출물과 `dist/theme.css`를 생성한다. `packages/ui/README.md`에는 설치, `@tcground/ui/theme.css` import, 기본 Button 사용법, local 검증, publish 절차를 추가했다. `apps/docs`는 `@tcground/ui/theme.css`가 `dist`를 바라보므로 `prebuild`/`prestart`에서 UI 패키지를 먼저 빌드하게 했다. `pnpm build:ui`, `pnpm --filter @tcground/ui pack --dry-run`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`(63/63), `pnpm build:docs`를 통과했다. `pack --dry-run` tarball에는 현재 `dist/components/ui/*`, `dist/index.*`, `dist/utils.*`, `dist/theme.css`, `package.json`, `README.md`만 포함되는 것을 확인했다.
- 2026-05-28: Dropdown Menu 문서 preview가 클릭해도 메뉴가 보이지 않는 문제를 수정했다. `DropdownMenuTrigger asChild`가 `Button`을 trigger로 사용할 때 Radix Popper가 trigger 위치를 측정하려면 Button이 ref를 DOM까지 전달해야 하는데, 기존 Button은 ref forwarding이 없어 menu content가 `translate(0, -200%)` 측정용 위치에 머물렀다. `Button`을 `React.forwardRef` 기반으로 바꿔 Radix `asChild` 조합을 안정화했고, Dropdown Menu 첫 preview는 강제 `defaultOpen` 대신 실제 클릭으로 열리는 예시로 정리했다. `http://localhost:3001/components/dropdown-menu`에서 첫 trigger 클릭 후 content wrapper가 trigger 아래 `translate(594.5px, 500.5px)` 위치로 배치되고 `aria-expanded="true"`가 되는 것을 확인했다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`(63/63), `pnpm build:ui`, `pnpm build:docs`, `pnpm build-storybook`을 통과했다.
- 2026-05-28: Button 이후 문서 컴포넌트의 Docusaurus preview fallback을 정리했다. `packages/ui/src/theme.css`에 현재 `data-slot` 마크업 기준 Dialog, Dropdown Menu, Tabs, Switch fallback 스타일을 추가해 Tailwind 생성 없이도 문서 preview에서 기본 스타일이 보이도록 했다. Dialog 문서는 `DialogContent`가 overlay와 close button을 포함하는 현재 구현 기준으로 정리했고, Dropdown Menu/Tabs 문서는 예전 namespace 예시(`*.Root`, `Tabs.Panel`)를 실제 named export API(`DropdownMenuTrigger`, `TabsContent` 등)로 교체했다. Switch 문서에는 styled UI library 기준 설명을 추가했다. `pnpm build:docs`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build:ui`, `pnpm build-storybook`을 통과했고, `http://localhost:3001` 문서 HTML에서 Dialog/Dropdown Menu/Tabs/Switch의 새 예시와 설명이 반영된 것을 확인했다.
- 2026-05-27: Docusaurus API Reference 표가 본문 폭을 넘어 오른쪽 TOC 영역까지 침범하는 문제를 수정했다. `PropsTable`에 column group을 추가하고, docs CSS에서 표를 `table-layout: fixed`와 본문 폭 기준으로 고정했다. 긴 union type code는 `overflow-wrap: anywhere`로 줄바꿈되게 하고, 모바일에서는 표 컨테이너 내부 가로 스크롤로 처리한다. `pnpm build:docs`, `pnpm lint`, `pnpm exec tsc --noEmit`을 통과했다.
- 2026-05-27: Docusaurus Button 문서 예시가 보이지 않는 문제를 수정했다. 예시 MDX와 build 산출물에는 `<Button>Save changes</Button>`과 variants/sizes 예시가 존재했지만, 문서 사이트는 Tailwind class 생성 없이 `@tcground/ui/theme.css` fallback에 의존하므로 현재 Button 마크업의 `data-slot='button'`에 스타일이 적용되지 않았다. `packages/ui/src/theme.css`에 `[data-slot='button']` variant/size/focus/disabled fallback과 Docusaurus dark theme token을 추가하고, docs custom CSS와 예시의 오래된 `--pokemon-*` 토큰을 현재 token contract로 교체했다. `pnpm build:docs`, `pnpm build:ui`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`(63/63), `pnpm build-storybook`을 통과했다.
- 2026-05-27: `@tcground/ui` Button의 기본 스타일을 설치 직후에도 완성도 있게 보이는 clean solid baseline으로 정리했다. 기존 variant API와 앱 특수 size(`search`, `auth`, `cta`, `tab`, `pill`)는 유지하고, 기본 size를 `h-9`, `rounded-md`, `px-4`, `text-sm`, subtle shadow 기준으로 낮췄다. `outline`, `secondary`, `ghost`, `destructive`, `link` 상태 스타일도 단순한 solid/flat 톤으로 정리했고, Storybook 기본 args와 Docusaurus Basic example은 `<Button>Save changes</Button>` 예시로 갱신했다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`(63/63), `pnpm build:ui`, `pnpm build-storybook`, `pnpm build:docs`, `pnpm build`를 통과했다. production build를 `http://localhost:3005`에서 확인해 기본 CTA가 36px 높이, subtle shadow, primary solid 스타일을 쓰고 검색 특수 size는 44px pill로 유지되는 것을 확인했다.
- 2026-05-27: `@tcground/ui` Button을 TCG 앱의 기본 action button으로 전환했다. Button default variant는 `--tcg-red: #bb001a`, hover는 `--tcg-red-dark: #930012`를 쓰도록 재정의했고, `search`, `auth`, `cta`, `tab`, `pill` size를 추가했다. native button 기본 `type='button'` 테스트와 explicit submit 테스트를 추가했으며, `HomeSearchForm`, `PublicHeader`, `LoginForm`, `SignupForm`, `/login`, `/cards/[cardId]`의 action button을 `Button`으로 치환했다. Storybook Button stories와 Docusaurus Button/theme 문서를 TCG token/light-dark 기준으로 갱신했다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`(63/63), `pnpm build:ui`, `pnpm build-storybook`, `pnpm build:docs`, `pnpm build`를 통과했다. production build를 `http://localhost:3005`에서 확인해 홈 검색/가입 버튼이 `#bb001a` 토큰을 쓰고, 로그인/상세 CTA/기간 탭이 `@tcground/ui` Button의 `data-variant`/`data-size`를 갖는 것을 확인했다.
- 2026-05-27: 기존 앱의 `components/ui/*` shadcn 기반 공통 UI 컴포넌트 24개와 Storybook stories를 `packages/ui/src/components/ui`로 이동해 `@tcground/ui` 패키지로 재구성하고, 중복 방지를 위해 기존 `components/ui` 디렉터리를 제거했다. 기존 직접 구현 Headless UI primitive와 관련 테스트/story는 제거하고, 패키지 내부 import는 앱 alias가 아닌 상대 경로와 `packages/ui/src/utils.ts`의 `cn` 유틸을 사용하도록 정리했다. 루트 앱은 `TooltipProvider`를 `@tcground/ui`에서 소비하며, Storybook은 `packages/ui` stories만 수집한다. Docusaurus는 `@tcground/ui` alias로 새 패키지를 바라보도록 조정했고 `Toggle` 예제는 기존 앱 UI에 있는 `Switch`로 전환했다. `components.json`의 shadcn alias도 새 패키지 위치로 맞췄다. `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test --run`(60/60), `pnpm build`, `pnpm build:ui`, `pnpm build-storybook`, `pnpm build:docs`를 통과했다.
- 2026-05-26: 접근성 중심 Headless UI 라이브러리와 Docusaurus 문서 사이트 1차 구현을 완료했다. 기존 Next 앱은 유지하고 workspace에 `packages/ui`와 `apps/docs`를 추가했다. `packages/ui`는 CSS 변수 기반 Pokemon theme tokens, Button, Dialog, Dropdown Menu, Tabs, Toggle을 직접 구현하고 React 18/19 peer range로 설정했다. Button은 `asChild`/disabled/variant/size를 지원하고, Dialog는 focus trap, Escape close, focus restore, `aria-modal`/label 연결을 포함한다. Dropdown Menu, Tabs, Toggle은 주요 ARIA 속성과 키보드 조작을 테스트로 검증한다. Storybook은 새 `Headless UI/Pokemon Components` 스토리를 수집하고, Docusaurus는 소개/설치/테마/접근성/컴포넌트/Radix 분석/라이브러리 비교/캐시 전략 문서를 포함한다. Docusaurus 최신 next 문서는 Node 24를 요구하므로 현재 Node 22 환경에서는 공식 3.9.2 문서의 Node 20+ 기준에 맞춰 `@docusaurus/*` 3.9.2를 사용했다.
- 2026-05-23: 배포 이미지 전송량 최적화를 완료했다. `next.config.ts`에 `assets.tcgdex.net`, `lh3.googleusercontent.com` remote image pattern과 크기 후보를 추가하고, 홈 카테고리 타일, 홈 인기 카드, 카테고리 카드, 인기 카드 목록, 상품 상세 카드 이미지를 `next/image`로 전환했다. 목록/홈/인기 카드 view model은 고해상도 `card_printings.image_url`보다 `cards.thumbnail_url`을 우선 사용하도록 바꿔 TCGdex `low.webp`를 먼저 전송한다. `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test --run`(60/60), `pnpm build`를 통과했고, `next start -p 3002` 기준 `/`, `/cards`, `/categories/pokemon`, `/cards/kr-004-charizard-ex-151`에서 `/_next/image` 최적화 URL 렌더를 확인했다.
- 2026-05-23: `/`와 `/cards` 배포 런타임 에러 대응을 완료했다. 두 페이지 모두 `getFeaturedPokemonCards` 실패 시 `console.error`로 원인을 남기고 빈 목록으로 fallback하도록 변경해 Supabase 카탈로그 조회 실패가 페이지 전체 500으로 번지지 않게 했다. 검증 중 로컬 production 서버에서 Supabase `Invalid API key`가 재현됐고, fallback 후 `/`와 `/cards`가 200 응답을 반환하는 것을 확인했다. `storybook-static/**`를 ESLint ignore에 추가해 정적 산출물 lint 실패를 제거했다. `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test --run`(60/60), `pnpm build`를 통과했고 `next start -p 3007` 기준 `/`, `/cards` 200 응답을 확인했다.
- 2026-05-22: Storybook 9 기반 UI 컴포넌트 카탈로그를 도입했다. `@storybook/nextjs-vite` 프레임워크로 `.storybook/main.ts`, `.storybook/preview.tsx`를 추가하고 `app/globals.css`와 `TooltipProvider`를 preview에 주입했다. `components/ui/*` shadcn 컴포넌트 24개 모두에 대해 기본/variant/주요 상태 스토리를 `*.stories.tsx`로 작성했다. `package.json`에 `storybook`, `build-storybook` 스크립트를 추가하고 `/storybook-static` 결과물을 `.gitignore`에 등록했다. `components/tcg/*` 도메인 컴포넌트 스토리와 Storybook MCP(`@storybook/addon-mcp`) 도입은 별도 후속 작업으로 남겼다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`(59/59), `pnpm build-storybook`을 통과했다.
- 2026-05-22: 검색 라우트를 카테고리 페이지로 흡수했다. `/search` 라우트와 mock 데이터를 제거하고, `lib/tcg-catalog.ts`의 `getPokemonCategoryPageData`에 `query` 옵션을 추가해 Supabase `cards.name`에 `ilike '%q%'` 필터를 적용하도록 했다. `/categories/[categoryId]`는 `searchParams.q`를 받아 `PublicHeader`에 `initialQuery`/`showClearButton`을 전달하고, `q`가 있을 때 검색 결과 인디케이터를 노출하면서 등록 세트 그리드를 숨긴다. 헤더 NAV에서 `검색` 메뉴를 제거했고, `HomeSearchForm` 제출은 `/categories/pokemon?q=...`로 이동한다. `/categories`와 `/cards`의 `/search` 인바운드 링크는 `/categories/pokemon`으로 정리했다. `PublicHeader`, `HomeSearchForm`, `app/categories/[categoryId]`, `lib/tcg-catalog`, `app/cards` 테스트를 새 동작에 맞춰 갱신했다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`(52/52), `pnpm build`를 통과했고 build route 목록에서 `/search`가 사라진 것을 확인했다.
- 2026-05-22: 포켓몬 카탈로그 이미지 enrichment를 완료했다. TCGdex REST API 확인 결과 한국어 endpoint는 seed secret rare 카드 이미지를 제공하지 않아, TCGdex 일본어 equivalent set/localId로 정확히 매칭되는 8장(`KR-003`~`KR-010`)만 Supabase MCP migration `enrich_pokemon_seed_images_tcgdex`로 보강했다. `cards.image_url`과 `card_printings.image_url`에는 `high.webp`, `cards.thumbnail_url`에는 `low.webp`를 저장하고, `card_printings.external_ids`에는 `tcgdex_id`, `image_source='tcgdex'`, `tcgdex_locale='ja'`, `tcgdex_match_basis='equivalent_japanese_set_local_id'`를 추가했다. `KR-001`, `KR-002`는 TCGdex CP6 set shell에 card rows/images가 없어 placeholder를 유지한다. Supabase 검증 결과 10개 seed printing 중 image URL과 `tcgdex_id`가 채워진 행은 8개다. 목록/상세 이미지 fallback 테스트를 추가했고 `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`(48/48), `pnpm build`를 통과했다. 기존 dev 서버 `http://localhost:3000`에서 `/categories/pokemon`은 8개 실제 이미지가 로드되고, `/cards/kr-004-charizard-ex-151`은 상세 이미지가 로드되며, `/cards/kr-001-charizard-ex`는 placeholder fallback을 유지하는 것을 확인했다.
- 2026-05-22: 포켓몬 카탈로그 seed 및 카테고리/상세 DB 전환을 완료했다. Supabase MCP migration `seed_pokemon_kr_catalog_sample`, `seed_pokemon_kr_catalog_cards`, `seed_pokemon_kr_catalog_printings_links`로 `memory-bank/price-source-validation.csv`의 `KR-001`~`KR-010` 한국판 대표 카드 10장을 `tcg_games`, `card_sets`, `cards`, `card_printings`, `card_categories`, `card_category_links`에 seed했다. 최종 row count는 `pokemon` game 1개, set 5개, card 10개, printing 10개, category 9개, category link 30개, price snapshot 0개다. `lib/tcg-catalog.ts`를 추가해 `/categories/pokemon`과 `/cards/[cardId]`를 Supabase 조회로 전환했고, 가격 snapshot은 seed하지 않고 UI view model에서 deterministic 표시값만 만든다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`(46/46), `pnpm build`를 통과했고 기존 dev 서버 `http://localhost:3000`에서 `/categories/pokemon`, `/cards/kr-004-charizard-ex-151`, `/cards/not-a-card`를 브라우저로 확인했다.
- 2026-05-22: 한국판 포켓몬 가격의 1차 자동 adapter source를 `ebay_sold`로 확정했다. eBay 공식 문서 기준 sold sales history는 Marketplace Insights API의 `/item_sales/search` 경로이고, `lastSoldDate`, condition, location, price currency 등 일부 filter로 후보를 좁힐 수 있다. 다만 Marketplace Insights는 restricted/limited release이며 신규 사용자에게 open되어 있지 않으므로, production adapter는 eBay Buy API/Marketplace Insights access와 API License Agreement 준수 범위 확인 이후에만 구현한다. 승인 전에는 eBay 페이지 scraping을 자동화하지 않고, 수동 CSV import와 adapter contract 설계만 진행한다.
- 2026-05-21: `memory-bank/price-source-validation.csv` 48개 행 기준으로 matching rule과 이상치 제거 기준을 확정했다. raw sold 표본 계산용 행은 38개, `confidence_score >= 0.8` 1차 snapshot 후보는 33개, damaged/played를 제외한 `clean_raw` snapshot 후보는 29개다. `KR-003`, `KR-004`, `KR-005`, `KR-006`, `KR-007`, `KR-008`은 clean raw snapshot 후보 3개 이상을 충족하고, `KR-001`, `KR-002`, `KR-009`, `KR-010`은 raw sold 전체는 3개 이상이지만 상태/신뢰도 필터 후 clean snapshot 표본이 3개 미만이므로 공개 평균 snapshot은 보류한다.
- 2026-05-21: `KR-002` M리자몽 EX 104/100의 부족 raw sold 표본을 보강했다. `memory-bank/price-source-validation.csv`에 Global Bunjang Sold Out raw 1개와 중고나라 판매완료 raw 1개를 추가해 총 48개 행이 됐다. `KR-002`는 전체 raw sold 3개를 충족했지만 eBay raw sold는 1개뿐이며, 추가 국내 표본은 정확한 sold timestamp 또는 collector number 텍스트가 불완전하므로 수동 검증 표본으로만 둔다.
- 2026-05-21: 부족했던 raw sold 표본 4개 샘플을 재확인했다. `memory-bank/price-source-validation.csv`에 5개 행을 추가해 총 46개 행이 됐다. `KR-001`은 eBay out-of-stock raw 행을 추가해 eBay raw sold 3개가 됐지만 sold timestamp가 불완전해 신뢰도 0.76으로 남겼다. `KR-008`은 Global Bunjang sold-out raw 1개를 보강해 전체 raw sold 3개가 됐다. `KR-009`는 eBay raw sold 1개와 중고나라 판매완료 raw 2개를 추가해 전체 raw sold 3개가 됐다. `KR-002`는 active listing, graded, bundle만 추가 확인돼 raw sold 1개로 미충족 상태를 유지한다.
- 2026-05-21: 한국판 포켓몬 가격 검증 샘플의 eBay sold 1차 추가 수집을 진행했다. `memory-bank/price-source-validation.csv`에 27개 행을 추가해 총 41개 행이 됐다. `KR-003` 리자몽 ex 흑염의 지배자, `KR-005` 뮤 ex 151, `KR-006` 피카츄 151 AR, `KR-007` 거북왕 ex 151, `KR-010` 님피아 ex 테라스탈 페스타는 eBay raw sold 표본 3개 이상을 확보했다. `KR-001`, `KR-002`, `KR-008`은 raw sold 표본이 3개 미만이고, `KR-009`는 raw sold 없이 graded/ended listing만 확인돼 추가 수집이 필요하다. bundle 거래, title/spec 언어 충돌, seller-ended listing은 제외 규칙으로 명시했다.
- 2026-05-21: `KR-004` 리자몽 ex 151 SAR부터 source별 수동 가격 표본 수집을 시작했다. `memory-bank/price-source-validation.csv`를 새로 만들고 KREAM, 중고나라, 번개장터, Guardian TCG, eBay sold 순서로 총 14개 행을 기록했다. KREAM은 graded 체결 3개, 번개장터는 raw sold out 3개, eBay sold는 raw sold 4개를 확보했고, Guardian TCG는 개별 sold가 아닌 집계 estimate라 제외 행으로 남겼다. 1차 자동 adapter 후보는 개별 sold item ID와 API 접근 가능성이 있는 `ebay_sold`를 우선하고, 국내 source는 공개 API/파트너 권한 확인 전까지 수동 import source로 유지하기로 했다.
- 2026-05-21: 한국판 포켓몬 가격 source 검증 단계를 `implementation-plan.md` 3.3으로 추가했다. 자동 crawler 구현 전에 source 평가 기준, 대표 카드 10장 수동 검증, source별 ToS/API/파트너 접근성 확인, CSV 수동 import 계약, matching rule과 이상치 제거 기준, 1차 adapter 대상 결정을 순서화했다. `prd/plan.md`에는 한국판 가격 수집 선행 검증 요구사항을 반영했고, 새 `trouble-shooting.md`에 source 평가표와 리스크를 기록했다.
- 2026-05-21: 한국판 포켓몬 가격 검증 샘플 10장을 확정하고, source별 공개 API/접근 가능성을 1차 확인했다. 샘플은 공식 포켓몬카드 한국 사이트의 상세 ID를 기준으로 리자몽 EX/M리자몽 EX, 리자몽 ex 2종, 뮤 ex, 피카츄 AR, 거북왕 ex, 가디안 ex, 블래키 ex, 님피아 ex를 포함한다. `price_observations` 필드에 맞춘 수동 import CSV 계약과 매칭/제외 규칙도 `trouble-shooting.md`에 기록했다. 국내 개인거래 source는 공개 API 확인 전까지 자동 crawler 대상에서 제외하고 수동 검증만 허용한다.
- 2026-05-21: `components/tcg/` 평탄 구조를 기능 도메인별 하위 폴더로 분리했다. 인증 폼/액션 4개는 `components/tcg/auth/`, 공개 헤더 2개는 `components/tcg/layout/`, 홈 검색 폼 2개는 `components/tcg/search/`로 이동하고, `app/**`의 9개 페이지 import와 `PublicHeader` 내부 cross-domain import를 `@/components/tcg/<sub>/...` 절대 경로로 갱신했다. 빈 `components/home/` 디렉터리는 제거했고, `architecture.md`에 새 디렉터리 구조와 same/cross sub-folder import 규칙을 명문화했다. 동작·UI 변경은 없으며 `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`(39/39), `pnpm build`를 통과했다. `grep -rn 'components/tcg/(PublicHeader|HomeSearchForm|LoginForm|SignupForm|logout-action)' app components`로 구 경로 잔존이 없음을 확인했다.
- 2026-05-21: MVP 헤더 메뉴 정리를 완료했다. `PublicHeader`의 1차 메뉴를 `홈 / 검색 / 카테고리 / 인기`로 바꾸고, 현재 경로 기준 active 상태를 `/`, `/search`, `/categories`, `/cards` 라우트에 맞게 계산하도록 정리했다. 깨진 링크 방지를 위해 `/categories` 대분류 목록 페이지와 `/cards` 인기 카드 목록 페이지를 추가했고, `lib/tcg-data.ts`의 정적 카테고리에 원피스를 추가했다. `/cards`는 정적 featured card 목록과 빈 상태 컴포넌트를 제공하며, 실제 Supabase `cards.is_featured = true` 조회 전환은 후속 데이터 연동 작업으로 유지한다. `PublicHeader`, `CategoryOverviewList`, `FeaturedCardsGrid` 테스트를 추가/갱신했고 `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`, `pnpm build`를 통과했다. dev 서버 `http://localhost:3000`에서 `/`, `/categories`, `/cards`가 200 응답을 반환하고 새 헤더 메뉴와 목록 콘텐츠가 렌더되는 것을 확인했다.
- 2026-05-20: Next.js App Router project-structure 기준에 맞춰 인증 route 내부 구조 정리를 완료했다. `app/login`과 `app/signup`에는 공개 route 파일인 `page.tsx`만 상위에 남기고, route 전용 서버 액션은 `_actions`, route 전용 유틸은 `_lib` private folder로 이동했다. `LoginForm`, `SignupForm`, page, action test, util test import 경로를 새 구조에 맞췄고, `app/auth/confirm/route.ts`와 `route.test.ts`는 route handler 테스트 co-location으로 유지했다. `memory-bank/architecture.md`, `memory-bank/conventions.md`, `memory-bank/implementation-plan.md`를 갱신했으며 `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`, `pnpm build`를 통과했다. Build route 목록에서 `_actions`, `_lib`는 route로 노출되지 않는 것을 확인했다.
- 2026-05-20: 인증 상태 기반 공개 헤더와 로그아웃을 완료했다. 홈, 검색 결과, 카테고리, 상품 상세의 중복 헤더를 서버 컴포넌트 `PublicHeader`로 교체하고 Supabase Auth `getClaims()`로 로그인 여부를 판별하게 했다. 비로그인 사용자는 현재 내부 경로를 보존한 `/login?next=...`, `/signup?next=...` 링크를 보고, 루트(`/`)에서는 쿼리 없는 `/login`, `/signup`을 본다. 로그인 사용자는 `로그아웃` 버튼만 보며, `components/tcg/logout-action.ts` 서버 액션은 `supabase.auth.signOut()` 후 항상 `/`로 이동한다. 헤더 인증 상태/안전한 링크/로그아웃 액션 테스트를 추가했고 `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`, `pnpm build`를 통과했다. 기존 dev 서버 `http://localhost:3000`에서 홈, 검색 결과, 카테고리, 상품 상세 HTML 응답의 헤더 렌더를 확인했다.
- 2026-05-20: 4.4 회원가입 Supabase Auth 연결을 완료했다. `/signup` 페이지와 `SignupForm`을 추가하고 서버 액션에서 이메일/비밀번호/비밀번호 확인 검증, 최소 8자 비밀번호 검증, `supabase.auth.signUp` 호출, 한국어 실패 메시지, 인증 메일 확인 성공 상태를 구현했다. 인증 진입점의 `next` 검증은 `lib/auth/redirect.ts`로 공통화해 외부 URL, protocol-relative URL, `/login`, `/signup`을 `/`로 fallback한다. `/auth/confirm` route handler는 `token_hash`, `type`, optional `next`를 받아 `supabase.auth.verifyOtp` 성공 시 안전한 내부 경로로 이동하고 실패/누락 시 `/login`으로 이동한다. 회원가입 검증/실패/성공과 인증 콜백 단위 테스트를 추가했고, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`, `pnpm build`를 통과했다.
- 2026-05-20: 4.2 로그인 Supabase Auth 연결을 완료했다. `/login`을 서버 컴포넌트 페이지와 클라이언트 `LoginForm`으로 분리하고, 서버 액션에서 이메일/비밀번호 필수·형식 검증, `supabase.auth.signInWithPassword`, 한국어 실패 메시지, 내부 `next` 또는 `/` 성공 리다이렉트를 구현했다. 이미 로그인된 사용자는 `/login` 접근 시 내부 `next` 또는 `/`로 이동하며, 외부/비정상 `next` 값과 `/login` 루프는 `/`로 fallback한다. 루트 `proxy.ts`를 추가해 Supabase SSR 세션 쿠키 갱신을 연결했고, 공개 페이지를 보호하지 않도록 middleware 유틸은 세션 갱신만 수행하게 정리했다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`, `pnpm build`를 통과했다.
- 2026-05-20: `memory-bank/implementation-plan.md`에 4.3 Storybook 컴포넌트 라이브러리 문서화 단계를 추가했다. 기본 Storybook 카탈로그 구축, `components/ui/*`와 `components/tcg/*` 스토리 작성, `storybook`/`build-storybook` 스크립트, 품질 게이트를 후속 작업으로 정리하고 Storybook MCP(`@storybook/addon-mcp`)는 기본 카탈로그 구축 이후 도입 여부를 검토하는 항목으로 분리했다.
- 2026-05-20: 포켓몬 우선 가격 데이터 수집 전략을 DB 모델에 반영했다. Supabase MCP migration `extend_price_collection_models`로 `card_printings`, `price_observations`, `price_collection_runs`를 추가하고 `card_price_snapshots`를 `card_printing_id` 기준으로 확장했다. 이어 `lock_internal_price_collection_tables` migration으로 원천 관측치와 source별 실행 로그에 명시적 deny-all RLS 정책을 적용했다. Supabase MCP로 테이블, FK, 인덱스, 정책, migration 기록을 확인했고 Security Advisor는 lint 없음, Performance Advisor는 아직 데이터/쿼리가 없어 신규 unused index 정보만 남았다. Rollback smoke test로 같은 Charizard의 `en`/`ja`/`ko` printing 3개, raw/PSA 10 관측치와 snapshot 2개, source 실패 run 1개가 구분 저장되는 것을 확인했다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`, 변경 문서 Prettier check를 통과했다.
- 2026-05-20: Supabase MCP migration `create_tcg_mvp_schema`로 MVP DB 스키마 7개 테이블(`tcg_games`, `card_sets`, `cards`, `card_categories`, `card_category_links`, `card_price_snapshots`, `favorite_cards`)과 FK, unique/index, `updated_at` trigger, RLS 정책을 실제 Supabase 프로젝트에 적용했다. 이어 `optimize_favorite_cards_rls_policies` migration으로 `favorite_cards` RLS 정책을 Supabase Performance Advisor 권장 형태로 최적화했다. Supabase MCP로 테이블, RLS, 정책, 인덱스, 트리거, migration 기록을 확인했고 Security Advisor는 lint 없음, Performance Advisor는 신규 미사용 인덱스 정보만 남았다.
- 2026-05-20: MVP 데이터 모델 상세 설계를 `memory-bank/db-schema.md`로 분리했다. `implementation-plan.md`는 3단계 체크리스트와 참조 링크만 남기고, `architecture.md`는 핵심 데이터 모델과 보안 기준 요약을 유지한다.
- 2026-05-20: MVP 데이터 모델 설계를 완료했다. Supabase Postgres 기준 테이블, 컬럼, 인덱스, 조회 기준, RLS 정책 방향을 문서화했고 `architecture.md`에는 핵심 데이터 모델과 보안 기준을 요약했다.
- 2026-05-20: 사용자가 제공한 Supabase 설정 지시에 따라 인증 수단을 Supabase Auth로 확정하고 `prd/plan.md`, `prd/login.md`, `implementation-plan.md`에 반영했다.
- 2026-05-20: `@supabase/supabase-js`, `@supabase/ssr`를 설치하고 shadcn Supabase Next.js 클라이언트 파일(`lib/supabase/client.ts`, `lib/supabase/server.ts`, `lib/supabase/middleware.ts`)을 추가했다. `.env.local`에는 사용자가 제공한 Supabase URL과 publishable key를 설정했다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`을 통과했다.
- 2026-05-20: `implementation-plan.md`와 `architecture.md`를 실제 라우트/페이지 구현 상태에 맞춰 동기화했다. 카테고리와 상품 상세 정적 UI는 완료로 표시하고, 로그인은 정적 화면 완료와 인증 동작 미완료를 분리했다. 다음 작업을 상품 상세 가격 차트 데이터 모델화로 정리했다.
- 2026-05-08: Stitch `TCGround Price Tracker`의 `TCGround | Card Detail (Updated Nav)` 화면과 `memory-bank/prd/product-detail.md` PRD를 기준으로 상품 상세 페이지(`app/cards/[cardId]/page.tsx`)를 구현했다. 동적 라우트로 `charizard-base-set-1st-edition` 슬러그에 Stitch HTML 기반 헤더(`HomeSearchForm size='header'`), `Base Set로 돌아가기` 단일 back 링크, 2컬럼 디테일(좌측 카드 이미지 / 우측 chip·display 가격·트렌드 칩·최저/최고가·관심 카드/가격 알림 액션·기간 탭이 달린 SVG 가격 차트·데이터 출처·마지막 업데이트), `Base Set의 다른 카드` 4개 columns masonry, 공통 footer를 구성했다. 알 수 없는 슬러그는 `notFound`로 분기한다. UI 라벨은 한국어, 카드명·세트·레어도는 영문 표기 컨벤션을 유지했다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`, `pnpm build`, 변경 파일 Prettier check를 통과했고 dev `http://localhost:3000/cards/charizard-base-set-1st-edition`에서 키 섹션 렌더(평균 거래가, 관심 카드 추가, 가격 변동(90일), Base Set의 다른 카드 등)와 `/cards/unknown-card` 404 응답을 확인했다.
- 2026-05-08: Stitch `TCGround Price Tracker`의 `TCGround | Category: Pokemon (Updated Nav)` 화면을 기준으로 카테고리 페이지(`app/categories/[categoryId]/page.tsx`)를 구현했다. 동적 라우트로 `pokemon` 슬러그에는 Stitch HTML을 따라 헤더(`HomeSearchForm size='header'`), Home > Categories > 포켓몬 breadcrumb, 한국어 hero, 사이드바(레어도 체크박스 / 시대 라디오), 5개 인기 세트 bento, 5개 인기 카드 columns masonry, footer를 구성했다. 알 수 없는 슬러그는 `notFound`로 분기하거나 `KNOWN_CATEGORY_LABELS`(magic, yugioh, one-piece)에 한해 "준비 중" 빈 상태를 노출한다. 카드 식별자는 영문, UI 라벨은 한국어 컨벤션을 따랐다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`, `pnpm build`, 변경 파일 Prettier check를 통과했고 dev `http://localhost:3050/categories/pokemon`과 `/categories/yugioh`에서 정상 상태와 빈 상태 렌더를 확인했다.
- 2026-05-08: Stitch `TCGround Price Tracker`의 `TCGround | Search: Charizard` 화면을 기준으로 검색 결과 페이지(`app/search/page.tsx`)를 구현했다. 헤더에는 prefilled 검색 입력(`HomeSearchForm size='header' showClearButton initialQuery=...`)을 두고, 정렬·필터 칩 스트립과 결과 카운트, 6개 정적 카드의 columns 기반 masonry, 빈 검색어 안내 상태를 구성했다. `HomeSearchForm`에 `initialQuery`/`showClearButton`/`size='header'` 변형을 추가하고 단위 테스트를 두 건 추가했다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`, `pnpm build`, 변경 파일 Prettier check를 통과했고 dev `http://localhost:3000/search?q=Charizard`와 `/search`에서 결과/빈 상태 렌더를 확인했다.
- 2026-05-08: Stitch `TCGround Price Tracker`의 `TCGround | Home (Search Optimized)` 화면을 기준으로 홈페이지 UI를 갱신했다. 히어로에 큰 한국어 검색 폼과 명시적 `검색` 버튼을 추가하고, 상단 내비게이션/카테고리/인기 카드/CTA/footer 문구를 대상 화면에 맞춰 한국어화했다. 검색은 빈 검색어를 막고 `/search?q=...`로 이동하는 기존 요구사항을 유지했다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`, `pnpm build`, 대상 파일 Prettier check를 통과했고, 기존 dev 서버 `http://localhost:3000`의 HTML 응답에서 핵심 섹션 렌더를 확인했다.
- 2026-05-08: Stitch `TCGround Price Tracker`의 `TCGround | Home` 화면을 기준으로 홈페이지 UI를 재구현했다. 상단 내비게이션, pill 검색창, `Collect with Confidence` 히어로, 4개 카테고리 타일, masonry `Trending Now`, CTA, footer를 Stitch HTML 구조와 문구에 맞췄다. `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test --run`, `pnpm build`를 통과했고 `next start -p 3002`의 HTML 응답에서 핵심 섹션 렌더를 확인했다.
- 2026-05-08: Stitch `TCGround Price Tracker` 디자인 시스템을 `app/globals.css`의 전역 토큰, shadcn semantic token, `tcg-*` component utility 구조로 반영했다. 루트 폰트는 Manrope 기반으로 맞췄고 `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`를 통과했다.
- 2026-05-08: 이전 세션의 TCGround 페이지별 PRD 초안을 `memory-bank/prd/*.md` 구조로 정리했다. 제품 전체 PRD의 MVP 범위도 로그인, 홈, 검색 결과, 카테고리, 상품 상세 중심으로 갱신했다.
- 2026-05-08: `AGENTS.md`의 PRD 참조 경로를 `memory-bank/prd-*.md`에서 `memory-bank/prd/*.md` 구조로 갱신했다.
- 2026-05-08: `memory-bank/prd/` 디렉터리 안의 PRD 파일명에서 중복 접두사 `prd-`를 제거하고 `AGENTS.md`와 실행 계획의 참조를 갱신했다.

## 의사결정 로그

- 2026-06-03: PriceCharting은 카드별 평균가/price guide가 아니라 historic sales 표의 개별 eBay completed-sale 행에 한해서 수동 evidence source로 허용한다. 이유는 eBay 원문과 sold 검색이 현재 환경에서 차단되지만, PriceCharting 표가 날짜·제목·가격이 있는 개별 eBay completed-sale row를 제공하기 때문이다. 단, 제3자 index라 직접 eBay item 원문보다 낮은 신뢰도로 보고 `source_name=pricecharting_ebay_sold`와 보수적 confidence를 사용한다.
- 2026-06-03: 현재 작업 환경에는 Supabase MCP와 Supabase CLI가 없어 원격 schema migration을 적용할 수 없었다. schema 변경은 MCP/CLI만 허용한다는 규칙을 지키기 위해 직접 SQL 실행은 하지 않고, 재현 가능한 migration SQL을 로컬에 추가한 뒤 가격 CSV는 기존 schema에 맞춰 legacy snapshot fallback으로 먼저 적재했다.
- 2026-06-03: 한국판 포켓몬 카탈로그는 현재 약 3,600개로 충분하므로 가격 데이터 작업에서 증설하지 않는다. 3,600개 전체 수동 sold 데이터 생성은 기술적으로 row를 만들 수 있더라도 검증 증거 없이 정확도를 해치므로 목표에서 제외한다. sold는 priority 카드 50~100개부터 source URL/item ID, 거래일, 원천 통화, 상태/variant, matching confidence가 있는 evidence 기반 관측치만 수동 CSV로 보강한다.
- 2026-06-03: 가격 source 확실성은 sold와 asking을 분리해 판단한다. P0 자동 source로 확실히 운영할 수 있는 것은 eBay Browse API의 판매중 호가 daily snapshot이며, 이는 sold 실거래가가 아니다. eBay Marketplace Insights는 sold 자동화의 공식 후보지만 restricted 상태이므로 승인 전 production adapter를 활성화하지 않는다. KREAM/번개장터/중고나라 등 국내 source는 공개 API/재사용 권한 확인 전까지 자동 수집하지 않고 수동 evidence 또는 partner-approved source로만 둔다.
- 2026-06-03: 외화 가격은 원천 통화와 원천 금액을 보존하고, 관측일 또는 snapshot 기준일 환율로 KRW 표시값을 계산한다. 환율 provider는 한국수출입은행 환율 OpenAPI를 1차 후보로 두며, rate date/provider/fetched_at을 저장해 UI에 환율 기준일을 표시한다.
- 2026-05-29: `@tcground/ui`의 직접 구현 전환은 전체 컴포넌트 일괄 재작성 대신 Button, Tabs, Dialog부터 진행한다. 이유는 이 셋이 `asChild` prop 병합, roving focus, modal focus trap이라는 접근성 primitive의 핵심 패턴을 대표하면서도, Select/Menu/Popover까지 한 번에 재작성하는 것보다 회귀 범위를 작게 통제할 수 있기 때문이다. 나머지 Radix 기반 wrapper는 후속 단계에서 컴포넌트별로 직접 구현 여부를 판단한다.
- 2026-05-28: npm 배포 준비에서는 기존 패키지 이름 `@tcground/ui`를 유지한다. 이유는 앱, Docusaurus, Storybook이 이미 같은 import 계약을 사용하고 있고, 이번 작업의 목표가 구조 변경이 아니라 배포 가능 상태 정리이기 때문이다. 실제 `npm publish`는 `tcground` npm scope 또는 organization 권한이 필요하므로 코드에서 자동 실행하지 않고 수동 단계로 남긴다.
- 2026-05-28: `@tcground/ui/theme.css`는 `src/theme.css`가 아니라 build 산출물인 `dist/theme.css`를 export한다. 이유는 npm tarball의 공개 계약을 `dist` 중심으로 단순화하고, 소비자가 package source 경로에 의존하지 않게 하기 위해서다. 이 변경으로 docs는 UI package build 결과가 필요하므로 `apps/docs`의 `prebuild`/`prestart`에서 `@tcground/ui`를 먼저 빌드한다.
- 2026-05-28: 루트 `tcground` 앱은 배포본 사용 검증을 위해 `@tcground/ui`를 npm semver range로 소비하고, `apps/docs`는 workspace dependency를 유지한다. 이유는 제품 앱에서 실제 외부 설치 계약을 검증하면서도 문서 사이트는 로컬 UI 패키지의 최신 개발 상태를 즉시 반영해야 하기 때문이다.
- 2026-05-27: Button의 "테마별 color"는 별도 `tone` prop이나 TCG 종목별 scope를 추가하지 않고, 기존 TCG semantic token과 light/dark token contract로 해결한다. 이유는 앱이 이미 `app/globals.css`에서 `--primary`, `--ring`, `--tcg-red`, `--tcg-red-dark`를 관리하고 있어 `@tcground/ui` Button이 같은 계약을 읽게 만드는 것이 앱 소비 전환과 문서화 모두에서 가장 작은 변경이기 때문이다.
- 2026-05-27: UI 라이브러리 과제의 방향을 “새로운 headless primitive를 별도 구현”에서 “기존 TCGround 앱의 `components/ui/*` 공통 UI 컴포넌트를 패키지화”로 변경한다. 이유는 사용자가 원하는 산출물이 기존 프로젝트 화면을 만들던 내부 UI 부품의 재사용 가능한 라이브러리화이기 때문이다. 따라서 `packages/ui`는 `@tcground/ui` 성격의 스타일드 UI 패키지로 재정의하고, 기존 `components/tcg/*` 도메인 컴포넌트는 앱 전용으로 유지한다. Storybook은 앱 도메인 컴포넌트가 아니라 `packages/ui` 라이브러리 컴포넌트의 상태 확인 도구로 좁힌다.
- 2026-05-26: 멘토 과제는 기존 TCGround 앱을 폐기하지 않고 모노레포 안의 별도 산출물로 진행한다. 이유는 현재 PRD와 구현이 TCG 가격 추적 서비스로 이미 축적돼 있고, Headless UI 과제는 `packages/ui`와 `apps/docs`로 분리할 때 기존 앱과 충돌 없이 npm 패키지/문서 사이트 형태를 보여줄 수 있기 때문이다.
- 2026-05-26: Headless UI 컴포넌트는 Radix UI를 런타임 의존성으로 감싸지 않고 직접 구현한다. Radix/shadcn은 접근성 구현 방식 분석과 비교 리포트의 참고 대상으로만 사용한다.
- 2026-05-26: Docusaurus는 `3.9.2`로 고정한다. 최신 next 문서는 Node 24.14 이상을 요구하지만 현재 로컬 환경은 Node 22.17.0이므로, 공식 3.9.2 문서의 Node 20+ 요구사항에 맞춰 빌드 가능한 버전을 선택했다. docs package는 Docusaurus 서버 번들 호환을 위해 `"type": "module"`을 두지 않는다.
- 2026-05-22: 별도 `/search` 라우트를 폐기하고 카드 이름 검색을 카테고리 페이지(`/categories/[categoryId]?q=...`)로 흡수한다. 이유는 두 페이지가 카드 그리드/가격 표시/필터를 거의 동일하게 갖고 있고, `/search`는 mock 데이터만 갖고 있어 카탈로그가 실제로 살아 있는 카테고리 페이지에 통합하는 편이 정보 구조와 코드 양쪽 모두를 단순하게 만들기 때문이다. 단일 TCG(포켓몬)만 Supabase에 들어와 있는 현 단계에서는 기본 카테고리를 `pokemon`으로 고정한다. 다중 TCG가 들어오면 cross-TCG 검색 진입점과 기본 카테고리 결정 규칙을 다시 정의한다.
- 2026-05-22: 포켓몬 seed 이미지 출처는 TCGdex를 우선하되, 한국어 endpoint가 secret rare 이미지 row를 제공하지 않는 경우 TCGdex 일본어 equivalent set/localId가 카드명/세트/번호를 만족하는 1:1 매칭일 때만 사용한다. 이 보강은 한국판 원본 이미지가 아니라 TCGdex 일본어 equivalent 이미지임을 `external_ids.tcgdex_locale='ja'`, `tcgdex_match_basis='equivalent_japanese_set_local_id'`로 명시한다. TCGdex에 equivalent card row/image가 없는 `KR-001`, `KR-002`는 다른 출처나 임의 이미지로 대체하지 않고 placeholder를 유지한다.
- 2026-05-22: 1차 자동 adapter source는 `ebay_sold`로 결정한다. 이유는 검증 샘플에서 개별 sold item ID, sold price, sold timestamp/date, condition 일부를 가장 구조화하기 쉽고, eBay 공식 Marketplace Insights API가 sold item sales history를 제공하는 경로이기 때문이다. 단, Marketplace Insights는 restricted/limited release이고 Buy API production access와 formal agreement가 필요하므로, 승인 전에는 eBay web page scraping이나 우회적 bulk 수집을 하지 않는다.
- 2026-05-22: `ebay_sold` raw snapshot 후보는 API filter 결과를 그대로 신뢰하지 않고 adapter 내부 매칭을 통과해야 한다. `price_kind=sold`, `variant=raw`, 단일 카드, 세트/번호/언어 일치, `confidence_score >= 0.8`, `lastSoldDate` 또는 equivalent sold timestamp 확보가 기본 조건이다. Graded/slab, bundle/lot/set/pack/box, language conflict, seller-ended 또는 sold timestamp 불완전 행은 snapshot input에서 제외하거나 observation only로 둔다.
- 2026-05-22: eBay 원천 데이터는 API License Agreement 검토 전 공개 UI에 raw listing 형태로 노출하지 않는다. 공개 UI는 허용 범위가 확인된 집계 snapshot, sample count, source attribution 수준으로 제한하고, seller/user 정보와 raw payload는 저장 최소화 또는 내부 감사용으로만 다룬다.
- 2026-05-22: 한국판 포켓몬 대표 카드 10장의 카탈로그 seed는 가격 데이터와 분리한다. `card_printings.external_ids`에는 `sample_id`, 공식 한국 set code, collector number만 저장하고, 이미지 URL은 확실한 재사용 가능 출처가 없으므로 `null`로 둔다. `card_price_snapshots`에는 가짜 가격을 넣지 않고, 실제 가격 adapter/snapshot 전까지 UI view model에서 deterministic 표시값만 계산한다.
- 2026-05-21: MVP 기본 raw 평균 snapshot은 `clean_raw` bucket만 사용한다. `clean_raw`는 `near_mint`, `light_played`, 신뢰 가능한 `unknown` condition으로 제한하고, `played`와 `damaged`는 원천 관측치로 보존하되 같은 bucket 표본이 3개 이상일 때만 별도 snapshot 후보로 본다. 필터와 이상치 제거 후 `sample_count < 3`이면 공개 평균 snapshot을 만들지 않는다.
- 2026-05-21: matching 판정은 hard reject, observation only, snapshot input 3단계로 확정한다. `exclude_reason`이 있는 행, listing/aggregate/ended/bundle/language conflict/graded-as-raw/세트·번호 부족 행은 snapshot에서 제외한다. `confidence_score < 0.8` 또는 exact sold timestamp가 불완전한 행은 관측치 후보로 보존하되 기본 snapshot 집계에서 제외한다.
- 2026-05-21: 10개 검증 샘플은 전체 raw sold 3개 이상을 충족한 것으로 보되, 자동 adapter source 결정에는 source별 표본 수를 따로 본다. `KR-002`, `KR-008`, `KR-009`는 국내 수동 source를 포함해야 최소 표본 수를 충족하므로 `ebay_sold` 단독 자동 adapter 품질 검증에서는 미충족 또는 낮은 신뢰 보강 샘플로 분리한다.
- 2026-05-21: `ebay_sold`는 1차 자동 adapter 후보로 유지하되, 자동 snapshot 집계 입력은 exact sold timestamp와 item ID를 구조화할 수 있는 eBay sold raw 행으로 제한한다. 국내 source는 `KR-002`, `KR-008`, `KR-009` 표본 보강에는 유효했지만 공개 API/파트너 권한이 확인되지 않아 자동 adapter가 아니라 수동 import source로만 둔다. source별 raw sold가 3개 미만인 샘플은 adapter 구현 후에도 `sample_count < 3` 상태로 낮은 신뢰도 또는 snapshot 보류 처리한다.
- 2026-05-21: eBay sold 1차 추가 수집 결과, `ebay_sold`는 한국판 raw 시세의 1차 자동 adapter 후보 지위를 유지한다. 단, adapter 집계 후보는 `price_kind=sold`, `variant=raw`, `exclude_reason` 없음, 단일 카드 listing, 언어/세트/번호 일치, 통화별 FX 처리 가능 조건을 만족하는 행으로 제한한다. Graded, bundle, language conflict, seller-ended listing은 raw snapshot 집계에서 제외한다.
- 2026-05-21: `KR-004` 1차 수동 수집 기준 1차 자동 adapter 후보는 `ebay_sold`를 최우선으로 둔다. 이유는 raw sold 표본이 3개 이상이고 item ID, sold price, sold date, condition 일부를 구조화할 수 있기 때문이다. Guardian TCG는 API와 집계값 접근성은 좋지만 개별 sold 표본이 아니라 가격 estimate source로 분리하고, KREAM/번개장터/중고나라는 표본 수가 있어도 공개 API/재사용 권한이 확인되지 않아 수동 import source로 유지한다.
- 2026-05-21: 한국판 포켓몬 가격 수집은 crawler부터 만들지 않고 source 검증과 수동 import 검증을 먼저 한다. 이유는 한국판 카드가 같은 카드명이라도 세트/번호/언어/finish/상태/그레이딩에 따라 가격이 크게 갈리고, 국내 거래처는 공개 API와 실거래 기록 접근성이 불균일하기 때문이다. MVP의 1차 신뢰 기준은 실거래성, ToS/접근 가능성, 카드 식별 정확도, 표본 수이며, 판매중 최저가는 보조 지표로만 사용한다.
- 2026-05-21: 한국판 가격 검증의 1차 자동 adapter 후보는 공개 API 또는 명시적 파트너 접근이 확인되는 source로 제한한다. KREAM, 번개장터, 중고나라, 당근은 국내 시세 감각에는 유용하지만 공개 API/재사용 조건이 불명확하므로 수동 import 검증 source로만 둔다. eBay와 Guardian TCG는 API 접근 경로가 있어 교차검증 후보로 두고, Cardmarket은 공식 API 신규 신청을 받지 않는 상태이므로 직접 adapter보다 price guide/API 접근 가능성 재확인 또는 제3자 집계 교차검증으로 둔다.
- 2026-05-21: `components/tcg/` 하위는 페이지/라우트 기준이 아니라 기능 도메인 기준(`auth/`, `layout/`, `search/`)으로 분리한다. 이유는 `PublicHeader`가 다중 페이지에서 공유돼 페이지 기준이 어느 한 라우트 폴더에 종속되면 깨지고, 인증 컴포넌트 3개가 이미 명확한 군집을 이루기 때문이다. App Router의 route 전용 파일은 `app/<route>/_components`로 두는 것이 표준이므로 `components/tcg/<page>/` 구조는 역할 충돌 가능성도 있다. 동일 하위 폴더 내부는 상대 import, cross-domain은 `@/components/tcg/<sub>/...` 절대 경로로 통일해 import 경로만 보고 도메인 경계를 알 수 있게 했다.
- 2026-05-21: MVP 공통 헤더의 1차 메뉴는 `홈 / 검색 / 카테고리 / 인기`로 확정한다. 기존 `세트`는 카테고리 하위 탐색 축으로 두고, `가격 가이드`는 실제 문서/가이드 콘텐츠가 생기기 전까지 최상위 메뉴에서 제외한다. `/categories`와 `/cards`는 헤더 링크가 깨지지 않도록 정적/seed 기반 목록 페이지로 먼저 제공하고, 실제 Supabase 조회는 후속 데이터 연동 작업에서 처리한다.
- 2026-05-20: 공개 페이지 헤더 인증 상태는 별도 클라이언트 상태나 새 프로필 조회 없이 Supabase Auth `getClaims()`로 판별한다. 로그인 상태에서는 아직 마이페이지/내 컬렉션 라우트가 없으므로 헤더 우측에는 `로그아웃`만 표시하고, 로그아웃 후 이동 경로는 항상 홈페이지(`/`)로 고정한다.
- 2026-05-20: 회원가입은 Supabase Auth 이메일 인증 방식으로 구현하고, 가입 액션 성공 시 즉시 세션을 신뢰하거나 내부 페이지로 보내지 않고 인증 메일 확인 안내를 표시한다. SSR 환경에서 확인 링크가 세션 쿠키를 설정하려면 Supabase Confirm signup 이메일 템플릿이 `/auth/confirm`으로 `token_hash`와 `type`을 전달해야 하며, `next` 보존은 `emailRedirectTo`/`{{ .RedirectTo }}` 기반으로 처리한다.
- 2026-05-20: Storybook은 우선 내부 개발/문서화 도구로만 도입한다. 제품 기능 변경, 외부 배포, Chromatic 연동은 초기 범위에서 제외하고, Storybook MCP(`@storybook/addon-mcp`)는 컴포넌트 기본 카탈로그가 완성된 뒤 AI 에이전트가 실제 stories/docs를 참조할 필요가 확인되면 도입을 검토한다.
- 2026-05-20: MVP 가격 수집은 포켓몬 우선, 실거래가 중심, source별 관측치 저장 후 일별 snapshot 집계 방식으로 확정한다. 카드 카탈로그는 TCGdex와 Pokémon TCG API 조합으로 시작하고, 북미 가격은 API 접근성이 좋은 TCGplayer/eBay 계열을 먼저 검토한다. 일본/한국판은 안정적인 공개 가격 API가 부족할 수 있으므로 수동 import와 ToS가 허용된 crawler/partner adapter를 분리해 붙인다.
- 2026-05-20: 기존 `cards`는 대표 카드로 유지하고 가격은 `card_printings` 기준으로 저장한다. 같은 카드명이라도 언어판, 지역판, 세트 코드, collector number, finish가 다르면 가격이 섞이지 않아야 하며, raw/graded와 상태/등급 구분은 `price_observations`와 `card_price_snapshots`의 `variant`, `condition_label`, `grade_company`, `grade_value` 축으로 분리한다.
- 2026-05-20: `price_observations`와 `price_collection_runs`는 원천 payload와 adapter 오류를 담을 수 있으므로 공개 읽기 대상에서 제외하고 명시적 deny-all RLS 정책을 둔다. 공개 UI는 검증/집계된 `card_price_snapshots`와 `card_printings`만 읽는다.
- 2026-05-20: DB 적용은 로컬 SQL 파일 추가 없이 Supabase MCP migration 기록(`create_tcg_mvp_schema`, `optimize_favorite_cards_rls_policies`, `extend_price_collection_models`, `lock_internal_price_collection_tables`)을 기준으로 관리한다. 현재 사용자의 요청 범위가 실제 Supabase 테이블 생성이고, Supabase 스키마/설정 변경은 MCP 또는 CLI로만 수행한다는 프로젝트 규칙을 따르기 때문이다.
- 2026-05-20: `updated_at` 컬럼이 있는 4개 테이블(`tcg_games`, `card_sets`, `cards`, `card_categories`)에는 `public.set_updated_at()` trigger를 적용한다. 문서의 컬럼 계약을 실제 수정 시각 추적 동작과 일치시키기 위함이다.
- 2026-05-20: `cards.set_id`와 `card_categories.parent_id`는 참조 대상 삭제 시 `set null`, 링크/가격/관심 카드처럼 종속성이 강한 테이블은 cascade 삭제로 적용한다. 운영 데이터의 상위 기준 삭제를 과도하게 전파하지 않으면서 연결성 테이블의 고아 행은 남기지 않기 위함이다.
- 2026-05-20: MVP DB는 `tcg_games`, `card_sets`, `cards`, `card_categories`, `card_category_links`, `card_price_snapshots`, `favorite_cards` 7개 테이블로 시작한다. 가격 요약은 별도 테이블로 중복 저장하지 않고 최신 `card_price_snapshots` 조회를 기준으로 하며, 성능 문제가 생기면 `card_price_summaries` view 또는 materialized view로 확장한다.
- 2026-05-20: 인증 수단은 Supabase Auth로 확정한다. 이유는 사용자가 Supabase 프로젝트 URL과 publishable key, `@supabase/supabase-js`, `@supabase/ssr`, shadcn Supabase 클라이언트 설치 지시를 제공했고, 기존 PRD의 인증 수단 미결 항목을 해소하기 때문이다.
- 2026-05-20: 로그인 페이지는 현재 정적 화면 구현 완료로만 간주하고 인증 동작은 별도 작업으로 진행한다. 인증 수단은 Supabase Auth로 확정됐으므로 다음 작업부터 Supabase 클라이언트 기준으로 로그인 요청/세션/리다이렉트 계약을 구현한다.
- 2026-05-08: 검색 결과 페이지 카드 명칭/세트/그레이딩 라벨은 영문 표기를 유지한다. 이유는 홈에서 이미 영문 카드 식별자를 사용 중이고 TCG 업계에서 영문 표기가 검색 매칭의 기준이기 때문이다. UI 라벨(필터, 가격: 높은순, 결과 카운트, 빈 상태 안내)은 한국어로 통일한다.
- 2026-05-08: 홈 UI는 기존 한국어 PRD 재구성안보다 Stitch `TCGround | Home` 및 `TCGround | Home (Search Optimized)` 화면을 우선 기준으로 둔다. 이유는 사용자가 Stitch 화면 1:1 참고를 명시했고, 해당 화면의 실제 레이아웃·문구·이미지가 구현 기준이기 때문이다.
- 2026-05-08: 홈 화면 링크 타깃은 검색 `/search?q=...`, 카테고리 `/categories/[categoryId]`, 카드 상세 `/cards/[cardId]`를 1차 기준으로 둔다. 이유는 PRD의 다음 이동 경로를 실제 라우트 골격과 연결하고, 이후 검색 결과/카테고리/상세 페이지 구현 시 같은 URL 계약을 재사용하기 위해서다.
- 2026-05-08: CSS 구조는 별도 CSS 파일을 늘리지 않고 `app/globals.css` 한 곳에서 Tailwind v4 `@theme inline`, semantic color token, `tcg-*` component utility 레이어로 관리한다. 이유는 shadcn 컴포넌트가 CSS 변수 기반이고, 현재 단계에서는 도메인 화면 공통 패턴을 빠르게 공유하는 것이 우선이기 때문이다.
- 2026-05-08: PRD 파일은 제품 전체 계획 파일(`plan.md`)과 페이지별 파일(`login.md`, `home.md`, `search-results.md`, `category.md`, `product-detail.md`)로 분리한다. 이유는 페이지 단위 구현 시 목표, 사용자 스토리, P0/P1 요구사항을 빠르게 참조하기 위해서다.
